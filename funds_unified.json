<!doctype html>
<html lang="he" dir="rtl">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>דשבורד חשיפות חיסכון | v3.38</title>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;500;700;900&family=Frank+Ruhl+Libre:wght@500;700;900&display=swap" rel="stylesheet" />
<style>
  :root {
    /* === Surfaces (cool, modern) === */
    --bg: #f4f6fa;
    --bg-soft: #eef1f7;
    --bg-deep: #e6eaf2;
    --card: #ffffff;
    --card-tint: #fbfcfe;

    /* === Ink === */
    --ink: #0f172a;
    --ink-soft: #334155;
    --ink-mid: #475569;
    --ink-muted: #64748b;
    --ink-faint: #94a3b8;

    /* === Lines === */
    --line: #e2e8f0;
    --line-soft: #eef2f7;
    --line-faint: #f4f6fa;

    /* === Accent (teal — financial trust) === */
    --accent: #0d9488;
    --accent-dark: #0f766e;
    --accent-soft: #ecfdf5;
    --accent-soft-2: #d1fae5;

    /* === Status === */
    --success: #059669;
    --success-soft: #d1fae5;
    --warning: #b45309;
    --warning-soft: #fef3c7;
    --danger: #dc2626;
    --danger-soft: #fee2e2;

    /* === Source categories (kept meaningful) === */
    --gemel-tag: #1d4ed8;
    --pensia-tag: #7c3aed;
    --bituach-tag: #15803d;

    /* === Exposure colors === */
    --stock: #f97316;
    --foreign: #2563eb;
    --fx: #16a34a;
    --liquid: #8b5cf6;

    /* === Performance === */
    --pos: #16a34a;
    --neg: #dc2626;

    /* === Shadows (layered) === */
    --shadow-xs: 0 1px 2px rgba(15, 23, 42, 0.04);
    --shadow-sm: 0 2px 8px rgba(15, 23, 42, 0.05), 0 1px 2px rgba(15, 23, 42, 0.04);
    --shadow-md: 0 8px 24px rgba(15, 23, 42, 0.08), 0 2px 6px rgba(15, 23, 42, 0.04);
    --shadow-lg: 0 18px 40px rgba(15, 23, 42, 0.10), 0 4px 12px rgba(15, 23, 42, 0.05);

    /* === Radii === */
    --r-xs: 8px;
    --r-sm: 12px;
    --r-md: 16px;
    --r-lg: 20px;
    --r-xl: 24px;
    --r-pill: 999px;
  }

  * { box-sizing: border-box; }
  html { -webkit-text-size-adjust: 100%; scroll-behavior: smooth; }
  html, body { margin: 0; padding: 0; }
  body {
    font-family: 'Heebo', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    background: var(--bg);
    color: var(--ink);
    line-height: 1.55;
    min-height: 100vh;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
    font-feature-settings: 'cv11', 'ss01';
  }
  ::selection { background: var(--accent); color: #fff; }

  /* === Layout === */
  .container { max-width: 1180px; margin: 0 auto; padding: 0 28px; }

  /* === Header (sticky, glass) === */
  header {
    border-bottom: 1px solid var(--line-soft);
    padding: 16px 0;
    background: rgba(244, 246, 250, 0.85);
    position: sticky;
    top: 0;
    z-index: 50;
    backdrop-filter: saturate(180%) blur(14px);
    -webkit-backdrop-filter: saturate(180%) blur(14px);
  }
  header .container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
  }
  .logo {
    font-family: 'Heebo', sans-serif;
    font-size: 26px;
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.025em;
    color: var(--ink);
    display: flex;
    align-items: center;
    gap: 2px;
  }
  .logo .accent {
    color: var(--accent);
    font-size: 1.3em;
    line-height: 0.6;
    margin-right: 1px;
  }
  .tagline {
    font-size: 10.5px;
    font-weight: 600;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: var(--ink-muted);
    direction: ltr;
    text-align: left;
    line-height: 1.5;
  }

  /* === Hero === */
  .hero {
    padding: 60px 0 44px;
    border-bottom: 1px solid var(--line-soft);
    background: linear-gradient(180deg, var(--card) 0%, var(--bg) 100%);
    position: relative;
    overflow: hidden;
  }
  .hero::before {
    content: '';
    position: absolute;
    top: -120px;
    right: -120px;
    width: 380px;
    height: 380px;
    background: radial-gradient(circle, rgba(13, 148, 136, 0.08), transparent 70%);
    pointer-events: none;
  }
  .hero .container { position: relative; z-index: 1; }
  .hero h1 {
    font-family: 'Heebo', sans-serif;
    font-size: 46px;
    font-weight: 900;
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin: 0 0 18px;
    max-width: 780px;
    color: var(--ink);
  }
  .hero h1 em {
    color: var(--accent);
    font-style: normal;
    background: linear-gradient(120deg, var(--accent) 0%, #14b8a6 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  .hero p {
    margin: 0;
    max-width: 680px;
    color: var(--ink-soft);
    font-size: 16.5px;
    line-height: 1.65;
    font-weight: 400;
  }
  .meta {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin-top: 26px;
    font-size: 13px;
    color: var(--ink-mid);
    font-variant-numeric: tabular-nums;
  }
  .meta span {
    background: var(--card);
    border: 1px solid var(--line);
    border-radius: var(--r-pill);
    padding: 8px 14px;
    box-shadow: var(--shadow-xs);
    line-height: 1.4;
    display: inline-flex;
    align-items: center;
    gap: 5px;
  }
  .meta strong { color: var(--ink); font-weight: 800; }

  .note {
    background: var(--accent-soft);
    border: 1px solid var(--accent-soft-2);
    border-radius: var(--r-md);
    padding: 14px 18px;
    margin-top: 22px;
    font-size: 13px;
    line-height: 1.65;
    color: #064e3b;
    max-width: 940px;
    position: relative;
  }
  .note::before {
    content: '';
    position: absolute;
    top: 14px;
    bottom: 14px;
    right: 0;
    width: 3px;
    background: var(--accent);
    border-radius: 2px;
  }

  /* === Sections === */
  .section { padding: 44px 0; }
  .section-title {
    font-family: 'Heebo', sans-serif;
    font-size: 28px;
    font-weight: 900;
    letter-spacing: -0.025em;
    line-height: 1.15;
    margin: 0 0 24px;
    display: flex;
    align-items: center;
    gap: 14px;
    color: var(--ink);
  }
  .section-title .num {
    font-family: 'Heebo', sans-serif;
    font-size: 11px;
    font-weight: 800;
    letter-spacing: 0.16em;
    color: var(--accent-dark);
    background: var(--accent-soft);
    padding: 5px 11px;
    border-radius: var(--r-pill);
    line-height: 1.3;
  }

  /* === Filter pills === */
  .filters {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-start;
    align-items: center;
    gap: 12px;
    margin: 0 0 28px;
  }
  .filter-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 12px 24px;
    min-height: 44px;
    font: inherit;
    font-size: 14px;
    font-weight: 800;
    background: var(--card);
    border: 1.5px solid var(--line);
    border-radius: var(--r-pill);
    color: var(--ink-soft);
    cursor: pointer;
    transition: all 0.18s ease;
    box-shadow: var(--shadow-sm);
    line-height: 1.2;
    text-align: center;
    white-space: nowrap;
    position: relative;
    -webkit-tap-highlight-color: transparent;
  }
  .filter-btn::before { display: none; }
  .filter-btn:hover:not(.active) {
    border-color: var(--ink-faint);
    color: var(--ink);
    background: #fff;
    box-shadow: var(--shadow-md);
    transform: translateY(-1px);
  }
  .filter-btn.active {
    background: var(--ink);
    color: #fff;
    border-color: var(--ink);
    box-shadow: var(--shadow-md);
  }
  .filter-btn:active { transform: translateY(0) scale(0.985); }
  .filter-count { display: none; }

  /* === Search === */
  .search-wrap { position: relative; max-width: none; }
  .search-wrap::before {
    content: "";
    position: absolute;
    top: 50%;
    right: 22px;
    transform: translateY(-50%);
    width: 16px;
    height: 16px;
    border: 2px solid var(--ink-faint);
    border-radius: 50%;
    pointer-events: none;
    z-index: 2;
    transition: border-color 0.18s ease;
  }
  .search-wrap::after {
    content: "";
    position: absolute;
    top: 50%;
    right: 14px;
    margin-top: 6px;
    transform: rotate(45deg);
    width: 8px;
    height: 2px;
    background: var(--ink-faint);
    border-radius: 1px;
    pointer-events: none;
    z-index: 2;
    transition: background-color 0.18s ease;
  }
  .search-wrap:focus-within::before { border-color: var(--accent); }
  .search-wrap:focus-within::after { background: var(--accent); }
  .search-input {
    width: 100%;
    padding: 18px 52px 18px 22px;
    font: inherit;
    font-size: 16px;
    font-weight: 500;
    border: 1.5px solid var(--line);
    background: var(--card);
    border-radius: var(--r-md);
    outline: none;
    transition: all 0.18s ease;
    box-shadow: var(--shadow-xs);
    color: var(--ink);
  }
  .search-input::placeholder { color: var(--ink-faint); font-weight: 400; }
  .search-input:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 4px rgba(13, 148, 136, 0.15), var(--shadow-sm);
  }
  .search-help {
    margin: 12px 4px 0;
    color: var(--ink-muted);
    font-size: 12.5px;
    line-height: 1.55;
  }

  /* === Search results === */
  .search-results {
    margin-top: 16px;
    background: transparent;
    max-height: none;
    overflow: visible;
    display: none;
    z-index: 1;
  }
  .search-results.active { display: block; }

  .result-summary {
    background: var(--accent-soft);
    border: 1px solid var(--accent-soft-2);
    border-radius: var(--r-md);
    padding: 14px 18px;
    margin-bottom: 12px;
  }
  .result-summary:hover { background: var(--accent-soft); }
  .result-summary .result-name {
    color: var(--accent-dark);
    font-weight: 800;
  }

  .result-item {
    background: var(--card);
    border: 1px solid var(--line-soft);
    border-radius: var(--r-md);
    padding: 14px 16px;
    margin-bottom: 10px;
    cursor: default;
    transition: all 0.18s ease;
    box-shadow: var(--shadow-xs);
  }
  .result-item:hover {
    border-color: var(--line);
    box-shadow: var(--shadow-sm);
    transform: translateY(-1px);
  }
  .result-item.selected {
    opacity: 0.65;
    background: var(--card-tint);
  }

  .result-main-row {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 14px;
    align-items: center;
  }
  .result-name {
    font-weight: 800;
    font-size: 14.5px;
    display: flex;
    gap: 7px;
    flex-wrap: wrap;
    align-items: center;
    margin-bottom: 5px;
    color: var(--ink);
    line-height: 1.4;
  }
  .result-meta {
    display: flex;
    gap: 11px;
    flex-wrap: wrap;
    color: var(--ink-mid);
    font-size: 12px;
    font-variant-numeric: tabular-nums;
    line-height: 1.5;
  }
  .result-counts {
    display: flex;
    gap: 7px;
    flex-wrap: wrap;
    margin-top: 8px;
    font-size: 11.5px;
    color: var(--ink-soft);
  }
  .result-count-pill {
    border: 1px solid var(--line);
    padding: 3px 10px;
    background: var(--card);
    border-radius: var(--r-pill);
    font-weight: 600;
  }

  /* === Tags === */
  .source-tag {
    display: inline-block;
    font-size: 9.5px;
    line-height: 1;
    font-weight: 800;
    letter-spacing: 0.06em;
    color: #fff;
    padding: 5px 8px;
    border-radius: var(--r-xs);
    white-space: nowrap;
  }
  .source-tag.gemelnet { background: var(--gemel-tag); }
  .source-tag.pensia { background: var(--pensia-tag); }
  .source-tag.bituach { background: var(--bituach-tag); }

  .selected-badge {
    display: inline-block;
    font-size: 10px;
    font-weight: 800;
    padding: 3px 9px;
    color: #065f46;
    background: var(--accent-soft-2);
    border-radius: var(--r-pill);
    white-space: nowrap;
  }
  .stale-badge, .micro-badge {
    display: inline-block;
    font-size: 9.5px;
    font-weight: 700;
    padding: 3px 8px;
    border-radius: var(--r-xs);
    white-space: nowrap;
    line-height: 1.35;
  }
  .stale-badge { color: #92400e; background: var(--warning-soft); }
  .micro-badge { color: var(--ink-muted); border: 1px solid var(--line); background: var(--card); }

  /* === Select button (primary action) === */
  .select-btn {
    border: 0;
    background: var(--ink);
    color: #fff;
    font: inherit;
    font-size: 12.5px;
    font-weight: 800;
    padding: 11px 20px;
    min-width: 80px;
    cursor: pointer;
    white-space: nowrap;
    border-radius: var(--r-sm);
    transition: all 0.18s ease;
    box-shadow: var(--shadow-sm);
    -webkit-tap-highlight-color: transparent;
  }
  .select-btn:hover {
    background: var(--accent);
    box-shadow: var(--shadow-md);
    transform: translateY(-1px);
  }
  .select-btn:active { transform: translateY(0); }
  .select-btn:disabled {
    cursor: default;
    background: var(--accent-soft-2);
    color: #065f46;
    box-shadow: none;
    transform: none;
  }


  /* === Quick update panel === */
  .quick-editor {
    display: none;
    margin: 16px 0 18px;
  }
  .quick-editor.active { display: block; }
  .quick-editor-card {
    background: var(--card);
    border: 1px solid var(--line);
    border-right: 4px solid var(--accent);
    border-radius: var(--r-md);
    box-shadow: var(--shadow-sm);
    padding: 16px 18px;
  }
  .quick-editor-title {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
    font-size: 15px;
    font-weight: 900;
    color: var(--ink);
    margin-bottom: 4px;
  }
  .quick-editor-meta {
    color: var(--ink-mid);
    font-size: 12.5px;
    margin-bottom: 14px;
    line-height: 1.5;
  }
  .quick-editor-form {
    display: grid;
    grid-template-columns: minmax(160px, 0.8fr) minmax(150px, 0.6fr) auto auto;
    gap: 10px;
    align-items: end;
  }
  .update-dashboard-btn {
    border: 0;
    background: var(--accent);
    color: #fff;
    font: inherit;
    font-size: 13.5px;
    font-weight: 900;
    padding: 13px 20px;
    border-radius: var(--r-sm);
    cursor: pointer;
    box-shadow: var(--shadow-sm);
    white-space: nowrap;
    transition: all 0.18s ease;
  }
  .update-dashboard-btn:hover {
    background: var(--accent-dark);
    transform: translateY(-1px);
    box-shadow: var(--shadow-md);
  }
  .quick-remove-btn {
    border: 1px solid var(--line);
    background: var(--card);
    color: var(--ink-muted);
    font: inherit;
    font-size: 13px;
    font-weight: 700;
    padding: 12px 14px;
    border-radius: var(--r-sm);
    cursor: pointer;
    white-space: nowrap;
  }
  .quick-remove-btn:hover {
    color: var(--danger);
    background: var(--danger-soft);
    border-color: rgba(220, 38, 38, 0.18);
  }
  .quick-editor-message {
    margin-top: 10px;
    color: var(--success);
    font-size: 12.5px;
    font-weight: 700;
  }
  .quick-action-row {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 10px;
  }
  .add-another-btn, .go-dashboard-btn, .reset-all-btn {
    border: 1px solid var(--accent-soft-2);
    background: var(--accent-soft);
    color: var(--accent-dark);
    font: inherit;
    font-size: 12px;
    font-weight: 800;
    padding: 8px 12px;
    border-radius: var(--r-pill);
    cursor: pointer;
    white-space: nowrap;
    transition: all 0.18s ease;
  }
  .add-another-btn:hover, .go-dashboard-btn:hover { background: var(--accent-soft-2); }
  .go-dashboard-btn {
    border-color: var(--line);
    background: var(--ink);
    color: #fff;
  }
  .go-dashboard-btn:hover {
    background: var(--accent);
    border-color: var(--accent);
  }
  .reset-all-btn {
    border-color: rgba(220, 38, 38, 0.20);
    background: var(--danger-soft);
    color: var(--danger);
  }
  .reset-all-btn:hover {
    background: #fecaca;
    border-color: rgba(220, 38, 38, 0.35);
  }

  /* === Fund card === */
  .funds-list { margin-top: 14px; }
  .fund-card {
    background: var(--card);
    border: 1px solid var(--line-soft);
    border-right: 4px solid var(--line);
    border-radius: var(--r-md);
    margin-bottom: 12px;
    padding: 18px 20px;
    display: grid;
    grid-template-columns: minmax(280px, 1.45fr) minmax(290px, 1fr) auto;
    gap: 26px;
    align-items: center;
    transition: all 0.2s ease;
    box-shadow: var(--shadow-xs);
  }
  .fund-card:hover { box-shadow: var(--shadow-sm); }
  .fund-card.gemelnet { border-right-color: var(--gemel-tag); }
  .fund-card.pensia { border-right-color: var(--pensia-tag); }
  .fund-card.bituach { border-right-color: var(--bituach-tag); }
  .fund-card.focused {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(13, 148, 136, 0.15), var(--shadow-md);
  }
  .fund-name {
    font-weight: 800;
    font-size: 15.5px;
    display: flex;
    gap: 8px;
    align-items: center;
    flex-wrap: wrap;
    margin-bottom: 5px;
    color: var(--ink);
    line-height: 1.4;
  }
  .fund-meta {
    font-size: 12.5px;
    color: var(--ink-mid);
    line-height: 1.5;
  }
  .fund-mini-exp {
    display: grid;
    grid-template-columns: repeat(5, minmax(54px, 1fr));
    gap: 8px;
    font-size: 13px;
    font-variant-numeric: tabular-nums;
  }
  .mini-exp {
    display: flex;
    flex-direction: column;
    align-items: center;
    background: var(--bg-soft);
    border: 1px solid var(--line-soft);
    border-radius: var(--r-sm);
    padding: 9px 4px 8px;
  }
  .mini-exp .label {
    font-size: 9.5px;
    color: var(--ink-muted);
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }
  .mini-exp .val {
    font-size: 15px;
    font-weight: 900;
    margin-top: 3px;
    color: var(--ink);
  }

  .balance-input-wrap {
    display: flex;
    align-items: flex-end;
    gap: 10px;
    flex-wrap: wrap;
    justify-content: flex-start;
  }

  .field-group {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .field-label {
    font-size: 10.5px;
    color: var(--ink-muted);
    font-weight: 800;
    letter-spacing: 0.04em;
  }
  .inline-input {
    display: flex;
    align-items: center;
    gap: 7px;
  }
  .fee-input {
    width: 102px;
    padding: 11px 12px;
    font: inherit;
    font-size: 16px;
    font-weight: 700;
    border: 1.5px solid var(--line);
    background: var(--card);
    text-align: left;
    direction: ltr;
    font-variant-numeric: tabular-nums;
    outline: none;
    border-radius: var(--r-sm);
    transition: all 0.18s ease;
    color: var(--ink);
  }
  .fee-input:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(13, 148, 136, 0.12);
  }

  .balance-input {
    width: 142px;
    padding: 11px 14px;
    font: inherit;
    font-size: 16px;
    font-weight: 700;
    border: 1.5px solid var(--line);
    background: var(--card);
    text-align: left;
    direction: ltr;
    font-variant-numeric: tabular-nums;
    outline: none;
    border-radius: var(--r-sm);
    transition: all 0.18s ease;
    color: var(--ink);
  }
  .balance-input:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(13, 148, 136, 0.12);
  }
  .currency {
    color: var(--ink-mid);
    font-weight: 700;
    font-size: 15px;
  }
  .remove-btn {
    background: transparent;
    border: 1px solid transparent;
    color: var(--ink-muted);
    cursor: pointer;
    padding: 8px 11px;
    font-size: 14px;
    border-radius: var(--r-xs);
    transition: all 0.18s ease;
    -webkit-tap-highlight-color: transparent;
    line-height: 1;
  }
  .remove-btn:hover {
    color: var(--danger);
    background: var(--danger-soft);
    border-color: rgba(220, 38, 38, 0.18);
  }

  /* === Empty state === */
  .empty {
    padding: 64px 24px;
    text-align: center;
    color: var(--ink-mid);
    background: var(--card);
    border: 1px dashed var(--line);
    border-radius: var(--r-lg);
  }
  .empty .arrow {
    font-family: 'Heebo', sans-serif;
    font-size: 44px;
    color: var(--ink-faint);
    margin-bottom: 10px;
    line-height: 1;
    font-weight: 300;
  }

  /* === Dashboard (dark, premium) === */
  .dashboard {
    background: #0f172a;
    color: #fff;
    padding: 60px 0 56px;
    display: none;
    position: relative;
    overflow: hidden;
  }
  .dashboard::before,
  .dashboard::after {
    display: none !important;
    content: none !important;
  }
  .dashboard .container { position: relative; z-index: 1; }
  .dashboard h2 {
    font-family: 'Heebo', sans-serif;
    font-size: 32px;
    font-weight: 900;
    margin: 0 0 8px;
    letter-spacing: -0.025em;
    line-height: 1.15;
    color: #fff;
  }
  .dashboard .subtitle {
    color: rgba(255, 255, 255, 0.6);
    font-size: 13.5px;
    margin-bottom: 28px;
    line-height: 1.55;
    font-weight: 400;
  }

  .total-row {
    display: grid;
    grid-template-columns: minmax(260px, 0.85fr) 1fr;
    gap: 32px;
    align-items: end;
    margin-bottom: 44px;
    padding-top: 28px;
    border-top: 1px solid rgba(255, 255, 255, 0.10);
  }
  .total {
    font-family: 'Heebo', sans-serif;
    font-size: 56px;
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.04em;
    font-variant-numeric: tabular-nums;
    color: #fff;
  }
  .total-label {
    color: rgba(255, 255, 255, 0.55);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    margin-top: 10px;
  }
  .source-mix {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    color: rgba(255, 255, 255, 0.92);
    font-size: 12.5px;
    font-weight: 600;
  }
  .source-mix-item {
    display: flex;
    align-items: center;
    gap: 8px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.10);
    border-radius: var(--r-pill);
    padding: 7px 13px;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  .dot {
    width: 9px;
    height: 9px;
    border-radius: 50%;
    display: inline-block;
    box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.10);
  }

  .metric-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-top: 28px;
  }
  .metric-card {
    border: 1px solid rgba(255, 255, 255, 0.10);
    background: rgba(255, 255, 255, 0.04);
    border-radius: var(--r-md);
    padding: 18px 18px 16px;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    transition: all 0.2s ease;
  }
  .metric-card:hover {
    border-color: rgba(255, 255, 255, 0.18);
    background: rgba(255, 255, 255, 0.06);
  }
  .metric-label {
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.62);
    margin-bottom: 14px;
    line-height: 1.4;
  }
  .metric-value {
    font-family: 'Heebo', sans-serif;
    font-size: 44px;
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.035em;
    font-variant-numeric: tabular-nums;
    color: #fff;
  }
  .metric-value .pct {
    font-size: 19px;
    font-weight: 600;
    margin-right: 4px;
    color: rgba(255, 255, 255, 0.55);
  }
  .metric-bar {
    height: 4px;
    background: rgba(255, 255, 255, 0.08);
    margin-top: 18px;
    overflow: hidden;
    border-radius: var(--r-pill);
  }
  .metric-bar-fill {
    height: 100%;
    width: 0;
    transition: width 0.7s cubic-bezier(0.16, 1, 0.3, 1);
    border-radius: var(--r-pill);
  }
  .stock-fill { background: linear-gradient(90deg, #fb923c 0%, #f97316 100%); }
  .foreign-fill { background: linear-gradient(90deg, #60a5fa 0%, #3b82f6 100%); }
  .fx-fill { background: linear-gradient(90deg, #4ade80 0%, #22c55e 100%); }
  .liquid-fill { background: linear-gradient(90deg, #c084fc 0%, #a855f7 100%); }

  .perf-wrap {
    margin-top: 46px;
    padding-top: 32px;
    border-top: 1px solid rgba(255, 255, 255, 0.10);
  }
  .perf-title {
    color: rgba(255, 255, 255, 0.92);
    font-weight: 800;
    margin-bottom: 6px;
    font-size: 18px;
    font-family: 'Heebo', sans-serif;
    letter-spacing: -0.01em;
  }
  .perf-note {
    color: rgba(255, 255, 255, 0.50);
    font-size: 12px;
    margin-bottom: 22px;
    line-height: 1.55;
  }
  .positive { color: #6ee7b7; }
  .negative { color: #fca5a5; }

  /* === Action toolbar === */
  .action-toolbar {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 12px;
    flex-wrap: wrap;
    margin: 10px 0 24px;
    max-width: 860px;
  }
  .action-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    border: 1px solid rgba(255, 255, 255, 0.22);
    background: rgba(255, 255, 255, 0.06);
    color: #fff;
    font: inherit;
    font-size: 14px;
    font-weight: 700;
    padding: 16px 18px;
    cursor: pointer;
    border-radius: var(--r-md);
    transition: all 0.2s ease;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    -webkit-tap-highlight-color: transparent;
    text-align: center;
    line-height: 1.3;
  }
  .action-btn::before {
    font-size: 20px;
    line-height: 1;
    flex-shrink: 0;
  }
  .action-btn[onclick*="saveAsPDF"]::before { content: "📄"; }
  .action-btn[onclick*="shareWhatsApp"]::before { content: "💬"; }
  .action-btn[onclick*="openProjection"]::before { content: "📈"; }
  .action-btn[onclick*="resetAllData"]::before { content: "↺"; }
  .action-btn:hover {
    border-color: rgba(255, 255, 255, 0.45);
    background: rgba(255, 255, 255, 0.16);
    transform: translateY(-2px);
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.20);
  }
  .action-btn:active { transform: translateY(0) scale(0.985); }
  .action-note {
    color: rgba(255, 255, 255, 0.55);
    font-size: 11.5px;
    line-height: 1.55;
    margin: -16px 0 28px;
    max-width: 760px;
  }

  /* === Details (table) === */
  .details {
    background: var(--bg-soft);
    padding: 56px 0;
    display: none;
  }
  .details h2 {
    font-family: 'Heebo', sans-serif;
    font-size: 28px;
    font-weight: 900;
    margin: 0 0 24px;
    letter-spacing: -0.025em;
    color: var(--ink);
  }
  .table-wrap {
    overflow-x: auto;
    background: var(--card);
    border: 1px solid var(--line-soft);
    border-radius: var(--r-md);
    box-shadow: var(--shadow-sm);
  }
  table {
    width: 100%;
    min-width: 900px;
    border-collapse: collapse;
    font-size: 13px;
    font-variant-numeric: tabular-nums;
  }
  th, td {
    padding: 13px 12px;
    border-bottom: 1px solid var(--line-soft);
    vertical-align: middle;
  }
  th {
    color: var(--ink-mid);
    font-size: 10.5px;
    font-weight: 800;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    text-align: right;
    white-space: nowrap;
    background: var(--card-tint);
  }
  td.num, th.num { text-align: left; direction: ltr; }
  tr:last-child td { border-bottom: none; }
  tbody tr { transition: background 0.15s ease; }
  tbody tr:hover { background: var(--bg-soft); }

  /* === Loading / footer === */
  .loading {
    padding: 70px 0;
    text-align: center;
    color: var(--ink-muted);
    font-family: 'Heebo', sans-serif;
    font-size: 18px;
    font-weight: 500;
  }
  footer {
    padding: 38px 0 32px;
    border-top: 1px solid var(--line-soft);
    color: var(--ink-mid);
    font-size: 12.5px;
    line-height: 1.7;
    background: var(--card);
  }
  footer strong { color: var(--ink-soft); font-weight: 700; }
  .data-update-meta {
    margin-top: 14px;
    font-size: 11.5px;
    color: var(--ink-muted);
    text-align: center;
    direction: rtl;
    font-weight: 600;
  }
  .developer-credit {
    margin-top: 10px;
    padding-top: 12px;
    border-top: 1px solid var(--line-soft);
    font-size: 11px;
    color: var(--ink-muted);
    text-align: center;
    direction: rtl;
    width: 100%;
    letter-spacing: 0.03em;
    font-weight: 500;
  }

  /* === Return search button === */
  .return-search-btn {
    border: 1px solid var(--accent-soft-2);
    background: var(--accent-soft);
    color: var(--accent-dark);
    font: inherit;
    font-size: 12.5px;
    font-weight: 700;
    padding: 10px 14px;
    cursor: pointer;
    white-space: nowrap;
    border-radius: var(--r-sm);
    transition: all 0.18s ease;
    -webkit-tap-highlight-color: transparent;
  }
  .return-search-btn:hover {
    border-color: var(--accent);
    background: var(--accent-soft-2);
    transform: translateY(-1px);
  }
  .floating-search-btn { display: none !important; }

  /* === Print === */
  @media print {
    @page { size: A4 landscape; margin: 6mm; }
    html, body {
      background: #fff !important;
      color: #111 !important;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
    }
    body { zoom: 0.82; }
    header, .hero, #searchSection, #portfolioSection, .floating-search-btn, .action-toolbar, .action-note {
      display: none !important;
    }
    .container { max-width: none; padding: 0 !important; }
    .dashboard, .details {
      display: block !important;
      background: #fff !important;
      color: #111 !important;
      padding: 0 0 7px !important;
    }
    .dashboard::before, .dashboard::after { display: none !important; }
    .dashboard h2 { color: #111 !important; font-size: 18px !important; margin: 0 0 2px !important; }
    .details h2 { color: #111 !important; font-size: 14px !important; margin: 0 0 4px !important; }
    .dashboard .subtitle { color: #444 !important; font-size: 9px !important; margin-bottom: 6px !important; }
    .total-label, .metric-label, .source-mix { color: #444 !important; }
    .total-row { grid-template-columns: 0.65fr 1fr; gap: 10px; margin-bottom: 8px; align-items: end; padding-top: 0; border-top: 0; }
    .total { color: #111 !important; font-size: 28px !important; }
    .total-label { font-size: 8px !important; margin-top: 2px !important; }
    .source-mix { font-size: 8px !important; gap: 6px !important; }
    .source-mix-item { background: transparent !important; border: 0 !important; padding: 0 !important; backdrop-filter: none !important; }
    .dot { width: 5px !important; height: 5px !important; box-shadow: none !important; }
    .metric-grid { grid-template-columns: repeat(4, 1fr); gap: 8px; margin-top: 6px !important; }
    .metric-card { border: 1px solid #bbb !important; background: transparent !important; padding: 5px !important; page-break-inside: avoid; border-radius: 4px !important; backdrop-filter: none !important; }
    .metric-label { font-size: 8px !important; margin-bottom: 3px !important; letter-spacing: 0.06em !important; }
    .metric-value { color: #111 !important; font-size: 24px !important; }
    .metric-value .pct { font-size: 10px !important; color: #666 !important; }
    .metric-bar { height: 2px !important; margin-top: 4px !important; background: #ddd !important; }
    .perf-wrap { margin-top: 10px !important; padding-top: 6px !important; border-top: 1px solid #bbb !important; }
    .perf-title { color: #111 !important; font-size: 11px !important; margin-bottom: 2px !important; }
    .perf-note { display: none !important; }
    .details { page-break-before: auto; background: #fff !important; }
    .table-wrap { overflow: visible !important; border-color: #bbb !important; box-shadow: none !important; border-radius: 0 !important; background: #fff !important; }
    table { min-width: 0 !important; width: 100% !important; table-layout: fixed !important; font-size: 7.5px !important; line-height: 1.15 !important; }
    th, td { padding: 2px 3px !important; border-bottom: 1px solid #ddd !important; vertical-align: top !important; }
    th { font-size: 6.8px !important; letter-spacing: 0.03em !important; background: transparent !important; }
    th:first-child, td:first-child { width: 31% !important; }
    .source-tag { color: #fff !important; font-size: 6px !important; padding: 2px 3px !important; border-radius: 2px !important; }
    footer { display: block !important; border-top: 1px solid #bbb !important; padding: 5px 0 0 !important; font-size: 7.5px !important; line-height: 1.25 !important; color: #444 !important; background: #fff !important; page-break-inside: avoid; }
    .data-update-meta { font-size: 7.5px !important; margin-top: 3px !important; color: #444 !important; text-align: center !important; }
    .developer-credit { font-size: 8px !important; margin-top: 4px !important; padding-top: 3px !important; color: #444 !important; text-align: center !important; width: 100% !important; }
  }

  /* === Tablet === */
  @media (max-width: 1000px) {
    .hero h1 { font-size: 38px; }
    .container { padding: 0 24px; }
    .metric-grid { gap: 12px; }
    .metric-value { font-size: 38px; }
    .total { font-size: 48px; }
  }

  /* === Mobile (< 760px) === */
  @media (max-width: 760px) {
    body { background: var(--bg); }
    .container { padding: 0 18px; }

    /* Header */
    header {
      padding: 12px 0;
      box-shadow: 0 1px 12px rgba(15, 23, 42, 0.04);
    }
    header .container {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
    }
    .logo {
      font-size: 22px;
      white-space: nowrap;
    }
    .tagline {
      max-width: 178px;
      font-size: 8.5px;
      line-height: 1.4;
      letter-spacing: 0.05em;
      opacity: 0.8;
    }

    /* Hero */
    .hero {
      padding: 22px 0 18px;
      border-bottom: 0;
      background: linear-gradient(180deg, #ffffff 0%, #f5f7fb 100%);
    }
    .hero::before {
      top: -80px;
      right: -80px;
      width: 240px;
      height: 240px;
    }
    .hero h1 {
      font-size: 25px;
      line-height: 1.18;
      letter-spacing: -0.02em;
      margin: 0 0 10px;
      max-width: none;
      font-weight: 900;
      text-wrap: balance;
    }
    .hero p {
      font-size: 13.5px;
      line-height: 1.55;
      color: var(--ink-mid);
    }
    .meta {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 7px;
      margin-top: 18px;
      font-size: 11.5px;
    }
    .meta span {
      padding: 9px 12px;
      border-radius: var(--r-sm);
      justify-content: flex-start;
      line-height: 1.4;
    }
    .note {
      border-radius: var(--r-sm);
      padding: 12px 14px;
      margin-top: 14px;
      font-size: 12px;
      line-height: 1.55;
      box-shadow: var(--shadow-xs);
    }

    /* Sections */
    .section { padding: 26px 0; }
    .section-title {
      font-size: 22px;
      line-height: 1.2;
      margin-bottom: 16px;
      gap: 10px;
    }
    .section-title .num {
      font-size: 9.5px;
      padding: 4px 9px;
    }

    /* Filters: compact mobile grid, no clipped pills */
    .filters {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 8px;
      padding: 0;
      margin: 0 0 14px;
      overflow: visible;
    }
    .filter-btn {
      width: 100%;
      padding: 10px 12px;
      min-height: 40px;
      font-size: 12px;
      box-shadow: var(--shadow-xs);
      border-radius: var(--r-pill);
      white-space: normal;
      line-height: 1.25;
    }
    .filter-btn.active { box-shadow: var(--shadow-sm); }

    /* Search */
    .search-input {
      padding: 16px 48px 16px 17px;
      font-size: 16px;
      border-radius: var(--r-md);
      box-shadow: var(--shadow-sm);
    }
    .search-wrap::before { right: 18px; width: 15px; height: 15px; }
    .search-wrap::after { right: 11px; }
    .search-help {
      background: rgba(255, 255, 255, 0.86);
      border: 1px solid var(--line-soft);
      border-radius: var(--r-sm);
      padding: 9px 11px;
      margin: 10px 0 0;
      font-size: 11.5px;
      line-height: 1.45;
      box-shadow: var(--shadow-xs);
    }

    /* Search results */
    .search-results { margin-top: 12px; }
    .result-summary { padding: 13px 14px; }
    .result-item {
      padding: 14px;
      box-shadow: var(--shadow-sm);
    }
    .result-main-row {
      display: grid;
      grid-template-columns: 1fr;
      gap: 12px;
    }
    .result-name {
      font-size: 14.5px;
      line-height: 1.4;
      margin-bottom: 7px;
    }
    .result-meta {
      display: grid;
      grid-template-columns: 1fr;
      gap: 5px;
      font-size: 12px;
      line-height: 1.45;
    }
    .result-counts {
      gap: 6px;
      font-size: 11px;
    }
    .select-btn {
      width: 100%;
      padding: 13px 16px;
      font-size: 14px;
    }


    .quick-editor-card { padding: 14px; border-right-width: 0; border-top: 4px solid var(--accent); }
    .quick-editor-title { font-size: 14.5px; }
    .quick-editor-form { grid-template-columns: 1fr; gap: 10px; }
    .update-dashboard-btn, .quick-remove-btn { width: 100%; padding: 13px 14px; }
    .quick-action-row { display: grid; grid-template-columns: 1fr; gap: 8px; }
    .add-another-btn, .go-dashboard-btn, .reset-all-btn { width: 100%; padding: 11px 12px; border-radius: var(--r-sm); }

    /* Portfolio cards */
    #portfolioSection { padding-top: 6px !important; }
    .funds-list { margin-top: 10px; }
    .fund-card {
      grid-template-columns: 1fr;
      gap: 14px;
      margin-bottom: 12px;
      padding: 16px;
      border: 1px solid var(--line);
      border-right: 1px solid var(--line);
      border-top: 4px solid var(--line);
      border-radius: var(--r-md);
      box-shadow: var(--shadow-sm);
    }
    .fund-card.gemelnet { border-top-color: var(--gemel-tag); }
    .fund-card.pensia { border-top-color: var(--pensia-tag); }
    .fund-card.bituach { border-top-color: var(--bituach-tag); }
    .fund-card.focused {
      border-color: var(--accent);
      border-top-color: var(--accent);
      box-shadow: 0 0 0 3px rgba(13, 148, 136, 0.15), var(--shadow-md);
    }
    .fund-name { font-size: 14.5px; line-height: 1.4; margin-bottom: 6px; }
    .fund-meta { font-size: 12px; line-height: 1.5; }
    .fund-mini-exp {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 8px;
    }
    .mini-exp {
      background: var(--bg-soft);
      border-radius: var(--r-sm);
      padding: 9px 6px;
    }
    .mini-exp .label { font-size: 9px; }
    .mini-exp .val { font-size: 14.5px; }

    .balance-input-wrap {
      width: 100%;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
      align-items: end;
    }
    .field-group { width: 100%; }
    .inline-input { width: 100%; }
    .balance-input, .fee-input {
      width: 100%;
      padding: 13px 12px;
      font-size: 16px;
      border-radius: var(--r-sm);
    }
    .currency { align-self: center; }
    .remove-btn {
      width: 44px;
      height: 44px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: var(--bg-soft);
      border: 1px solid var(--line);
      border-radius: var(--r-sm);
    }
    .return-search-btn {
      grid-column: 1 / -1;
      padding: 12px;
      font-size: 13px;
    }
    .empty {
      padding: 40px 18px;
      border-radius: var(--r-md);
    }

    /* Dashboard */
    .dashboard {
      padding: 32px 0 36px;
      margin-top: 8px;
      background: #0f172a !important;
    }
    .dashboard h2 { font-size: 26px; margin-bottom: 5px; }
    .dashboard .subtitle { font-size: 12px; line-height: 1.55; margin-bottom: 18px; }
    .action-toolbar {
      display: grid;
      grid-template-columns: 1fr;
      gap: 10px;
      margin: 10px 0 18px;
      max-width: none;
    }
    .action-btn {
      width: 100%;
      padding: 16px 16px;
      font-size: 14.5px;
      gap: 12px;
    }
    .action-btn::before { font-size: 22px; }
    .action-note { font-size: 11px; line-height: 1.55; margin: 0 0 22px; }
    .total-row {
      grid-template-columns: 1fr;
      gap: 16px;
      margin-bottom: 24px;
      padding-top: 22px;
    }
    .total { font-size: 42px; }
    .total-label { font-size: 9.5px; letter-spacing: 0.16em; }
    .source-mix {
      display: grid;
      grid-template-columns: 1fr;
      gap: 7px;
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.10);
      border-radius: var(--r-sm);
      padding: 11px;
    }
    .source-mix-item {
      background: transparent;
      border: 0;
      padding: 0;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      font-size: 13px;
    }
    .metric-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
      margin-top: 14px;
    }
    .metric-card { padding: 14px 13px; border-radius: var(--r-sm); }
    .metric-label { font-size: 9.5px; letter-spacing: 0.10em; margin-bottom: 10px; }
    .metric-value { font-size: 30px; }
    .metric-value .pct { font-size: 14px; }
    .metric-bar { height: 3px; margin-top: 12px; }
    .perf-wrap { margin-top: 28px; padding-top: 22px; }
    .perf-title { font-size: 16px; }
    .perf-note { font-size: 11px; line-height: 1.5; margin-bottom: 14px; }

    /* Details */
    .details { padding: 30px 0; }
    .details h2 { font-size: 22px; margin-bottom: 14px; }
    .table-wrap { background: var(--card); }
    table { min-width: 760px; font-size: 12px; }
    th, td { padding: 11px 9px; }
    th { font-size: 9.5px; }

    footer {
      padding: 28px 0 30px;
      font-size: 12px;
      line-height: 1.7;
    }
    .developer-credit {
      margin-top: 14px;
      padding-top: 12px;
      font-size: 11px;
    }
  }

  /* === Small mobile === */
  @media (max-width: 420px) {
    .container { padding: 0 16px; }
    .filters { grid-template-columns: 1fr; padding: 0; margin: 0 0 14px; gap: 7px; }
    .tagline {
      max-width: 158px;
      font-size: 8px;
    }
    .logo { font-size: 20px; }
    .hero h1 { font-size: 25px; }
    .meta { grid-template-columns: 1fr; }
    .metric-value { font-size: 26px; }
    .metric-value .pct { font-size: 12px; }
    .metric-label { font-size: 9px; }
    .total { font-size: 36px; }
    .fund-mini-exp { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .section-title { font-size: 20px; }
    .dashboard h2 { font-size: 22px; }
  }

  /* === Reduced motion === */
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      transition-duration: 0.01ms !important;
    }
    html { scroll-behavior: auto; }
  }



  /* === PDF / Print light report override ===
     The on-screen dashboard can stay dark, but the printed PDF must be bright,
     clean and economical for A4. */
  @media print {
    body.pdf-mode .dashboard,
    .dashboard {
      background: #ffffff !important;
      color: #0f172a !important;
      box-shadow: none !important;
    }
    body.pdf-mode .dashboard *,
    .dashboard * {
      text-shadow: none !important;
    }
    body.pdf-mode .dashboard h2,
    body.pdf-mode .perf-title,
    .dashboard h2,
    .perf-title {
      color: #0f172a !important;
    }
    body.pdf-mode .dashboard .subtitle,
    body.pdf-mode .action-note,
    body.pdf-mode .perf-note,
    .dashboard .subtitle,
    .action-note,
    .perf-note {
      color: #475569 !important;
    }
    body.pdf-mode .total,
    body.pdf-mode .metric-value,
    .total,
    .metric-value {
      color: #0f172a !important;
    }
    body.pdf-mode .total-label,
    body.pdf-mode .metric-label,
    .total-label,
    .metric-label {
      color: #64748b !important;
    }
    body.pdf-mode .metric-card,
    .metric-card {
      background: #f8fafc !important;
      border: 1px solid #dbe3ee !important;
      color: #0f172a !important;
      box-shadow: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    body.pdf-mode .source-mix-item,
    .source-mix-item {
      background: #f8fafc !important;
      border: 1px solid #dbe3ee !important;
      color: #0f172a !important;
      box-shadow: none !important;
    }
    body.pdf-mode .metric-bar,
    .metric-bar {
      background: #e2e8f0 !important;
    }
    body.pdf-mode .action-toolbar,
    body.pdf-mode .action-note,
    .action-toolbar,
    .action-note {
      display: none !important;
    }
  }


  /* === Forced light PDF mode ===
     Important for mobile browsers: some iOS share/print flows capture the current
     rendered page before applying @media print. Therefore this class changes the
     visible dashboard to a light report before window.print() is opened. */
  body.pdf-mode {
    background: #ffffff !important;
    color: #0f172a !important;
  }
  body.pdf-mode header,
  body.pdf-mode .hero,
  body.pdf-mode #searchSection,
  body.pdf-mode #portfolioSection,
  body.pdf-mode .action-toolbar,
  body.pdf-mode .action-note,
  body.pdf-mode .floating-search-btn {
    display: none !important;
  }
  body.pdf-mode .dashboard,
  body.pdf-mode .details {
    display: block !important;
    background: #ffffff !important;
    color: #0f172a !important;
    box-shadow: none !important;
  }
  body.pdf-mode .dashboard::before,
  body.pdf-mode .dashboard::after {
    display: none !important;
  }
  body.pdf-mode .dashboard h2,
  body.pdf-mode .details h2,
  body.pdf-mode .perf-title,
  body.pdf-mode .total,
  body.pdf-mode .metric-value {
    color: #0f172a !important;
  }
  body.pdf-mode .dashboard .subtitle,
  body.pdf-mode .perf-note,
  body.pdf-mode .total-label,
  body.pdf-mode .metric-label {
    color: #475569 !important;
  }
  body.pdf-mode .metric-card,
  body.pdf-mode .source-mix,
  body.pdf-mode .source-mix-item,
  body.pdf-mode .table-wrap {
    background: #f8fafc !important;
    border-color: #dbe3ee !important;
    color: #0f172a !important;
    box-shadow: none !important;
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
  }
  body.pdf-mode .metric-bar {
    background: #e2e8f0 !important;
  }
  body.pdf-mode th {
    background: #f8fafc !important;
    color: #475569 !important;
  }
  body.pdf-mode td,
  body.pdf-mode footer,
  body.pdf-mode .developer-credit,
  body.pdf-mode .data-update-meta {
    color: #0f172a !important;
  }


  /* v3.26: hide section number badge next to search title */
  .section-title .num { display: none !important; }

  /* v3.28: 4 blue info cubes replacing hero prose */
  .info-cubes {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 12px;
    margin-top: 22px;
  }
  @media (max-width: 720px) {
    .info-cubes {
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
    }
  }
  .info-cube {
    background: #eff6ff;
    border: 2px solid #bfdbfe;
    border-radius: 14px;
    padding: 18px 12px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 10px;
    min-height: 120px;
    text-align: center;
    box-shadow: 0 1px 2px rgba(37, 99, 235, 0.05);
    /* v3.29: make interactive */
    cursor: pointer;
    font-family: inherit;
    font-size: inherit;
    color: inherit;
    width: 100%;
    transition: background 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease, transform 0.15s ease;
    -webkit-tap-highlight-color: transparent;
  }
  .info-cube:active {
    transform: scale(0.98);
  }
  .info-cube:focus-visible {
    outline: 3px solid #2563eb;
    outline-offset: 2px;
  }
  .info-cube[aria-expanded="true"] {
    background: #dbeafe;
    border-color: #2563eb;
    box-shadow: 0 4px 14px rgba(37, 99, 235, 0.18);
  }
  @media (hover: hover) {
    .info-cube:hover {
      background: #dbeafe;
      border-color: #60a5fa;
    }
  }
  .info-cube-icon {
    font-size: 28px;
    line-height: 1;
    pointer-events: none;
  }
  .info-cube-text {
    font-size: 13.5px;
    font-weight: 700;
    color: #1e3a8a;
    line-height: 1.45;
    pointer-events: none;
  }
  /* v3.29: tooltip panel below the cubes */
  .info-cube-tooltip {
    margin-top: 14px;
    padding: 18px 20px;
    background: #1e3a8a;
    color: #ffffff;
    border-radius: 14px;
    font-size: 14px;
    line-height: 1.7;
    text-align: right;
    box-shadow: 0 10px 30px rgba(30, 58, 138, 0.22);
    animation: cubeTipIn 0.22s ease;
  }
  .info-cube-tooltip[hidden] {
    display: none;
  }
  .info-cube-tooltip strong {
    color: #fff;
    font-weight: 800;
    display: block;
    margin-bottom: 6px;
    font-size: 15.5px;
  }
  @keyframes cubeTipIn {
    from { opacity: 0; transform: translateY(-6px); }
    to   { opacity: 1; transform: translateY(0); }
  }


  /* === Entry disclosure gate === */
  body.disclosure-gate-open {
    overflow: hidden;
  }
  .entry-disclosure-overlay {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 22px;
    background: rgba(15, 23, 42, 0.72);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }
  .entry-disclosure-overlay[hidden] {
    display: none !important;
  }
  .entry-disclosure-card {
    width: min(760px, 100%);
    max-height: min(86vh, 760px);
    overflow-y: auto;
    background: #ffffff;
    border: 1px solid var(--line);
    border-radius: 24px;
    box-shadow: 0 24px 80px rgba(15, 23, 42, 0.28);
    padding: 28px;
    color: var(--ink);
  }
  .entry-disclosure-kicker {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 12px;
    border-radius: 999px;
    background: var(--accent-soft);
    color: var(--accent-dark);
    font-size: 13px;
    font-weight: 800;
    margin-bottom: 12px;
  }
  .entry-disclosure-card h2 {
    margin: 0 0 10px;
    font-size: 28px;
    line-height: 1.18;
    letter-spacing: -0.02em;
  }
  .entry-disclosure-lead {
    margin: 0 0 18px;
    color: var(--ink-soft);
    line-height: 1.7;
    font-size: 15px;
  }
  .entry-disclosure-list {
    display: grid;
    gap: 10px;
    margin: 0 0 22px;
  }
  .entry-disclosure-item {
    background: var(--bg-soft);
    border: 1px solid var(--line-soft);
    border-radius: 16px;
    padding: 13px 15px;
    font-size: 14px;
    line-height: 1.65;
    color: var(--ink-soft);
  }
  .entry-disclosure-item strong {
    color: var(--ink);
    font-weight: 900;
  }
  .entry-disclosure-confirm {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    margin: 0 0 18px;
    padding: 14px 16px;
    background: #f8fafc;
    border: 1px solid var(--line);
    border-radius: 16px;
    color: var(--ink-soft);
    font-size: 14px;
    line-height: 1.55;
    cursor: pointer;
  }
  .entry-disclosure-confirm input {
    width: 18px;
    height: 18px;
    margin-top: 2px;
    accent-color: var(--accent);
    flex: 0 0 auto;
  }
  .entry-disclosure-actions {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    flex-wrap: wrap;
  }
  .entry-disclosure-button {
    border: 0;
    border-radius: 14px;
    padding: 13px 22px;
    background: var(--accent);
    color: #ffffff;
    font-family: inherit;
    font-size: 15px;
    font-weight: 900;
    cursor: pointer;
    transition: opacity .2s, transform .2s, background .2s;
  }
  .entry-disclosure-button:hover:not(:disabled) {
    background: var(--accent-dark);
    transform: translateY(-1px);
  }
  .entry-disclosure-button:disabled {
    opacity: .45;
    cursor: not-allowed;
  }
  .entry-disclosure-small {
    color: var(--ink-muted);
    font-size: 12px;
    line-height: 1.5;
  }
  @media (max-width: 640px) {
    .entry-disclosure-overlay { padding: 14px; align-items: flex-start; overflow-y: auto; }
    .entry-disclosure-card { padding: 20px; max-height: none; margin: 12px 0; }
    .entry-disclosure-card h2 { font-size: 23px; }
    .entry-disclosure-actions { display: grid; }
    .entry-disclosure-button { width: 100%; }
  }

</style>
</head>
<body>

<div class="entry-disclosure-overlay" id="entryDisclosureOverlay" role="dialog" aria-modal="true" aria-labelledby="entryDisclosureTitle">
  <div class="entry-disclosure-card">
    <div class="entry-disclosure-kicker">גילוי נאות</div>
    <h2 id="entryDisclosureTitle">לפני הכניסה לדשבורד חשיפות החיסכון</h2>
    <p class="entry-disclosure-lead">
      הכלי נועד לתת תמונת עזר על חשיפות, ביצועים ודמי ניהול במוצרי חיסכון, על בסיס נתונים ציבוריים ונתונים שהמשתמש מזין ידנית. הוא אינו מחליף בדיקה מקצועית או ייעוץ אישי.
    </p>
    <div class="entry-disclosure-list">
      <div class="entry-disclosure-item"><strong>אין ייעוץ או המלצה:</strong> המידע אינו מהווה ייעוץ השקעות, שיווק השקעות, ייעוץ פנסיוני, ייעוץ מס או המלצה לביצוע פעולה כלשהי.</div>
      <div class="entry-disclosure-item"><strong>מקור הנתונים:</strong> הנתונים מבוססים על פרסומים ציבוריים של רשות שוק ההון ומאגרי גמל נט, פנסיה נט וביטוח נט. ייתכנו פערי עדכון, נתונים חסרים או אי דיוקים.</div>
      <div class="entry-disclosure-item"><strong>נתונים אישיים:</strong> הדשבורד אינו מושך מידע אישי ממסלקה פנסיונית. היתרות ודמי הניהול שהוזנו נשמרים בדפדפן בלבד, אינם נשמרים בשרת ואינם משויכים לפרטים אישיים.</div>
      <div class="entry-disclosure-item"><strong>סימולציה ותשואות עבר:</strong> כל סימולציה המבוססת על נתוני עבר היא להמחשה בלבד. ביצועי עבר אינם מנבאים את העתיד ואינם מהווים תחזית.</div>
      <div class="entry-disclosure-item"><strong>אחריות:</strong> השימוש בכלי ובמסקנות ממנו הוא באחריות המשתמש בלבד. לפני כל פעולה פיננסית או פנסיונית יש להיוועץ בבעל רישיון מתאים.</div>
    </div>
    <label class="entry-disclosure-confirm">
      <input type="checkbox" id="entryDisclosureCheck" />
      <span>קראתי והבנתי שהמידע מיועד להמחשה בלבד ואינו מהווה ייעוץ, שיווק, המלצה או תחליף לבדיקה מקצועית אישית.</span>
    </label>
    <div class="entry-disclosure-actions">
      <button type="button" class="entry-disclosure-button" id="entryDisclosureAccept" disabled>מאשר ונכנס לדשבורד</button>
      <div class="entry-disclosure-small">האישור נשמר רק למשך הסשן הנוכחי בדפדפן.</div>
    </div>
  </div>
</div>

<section class="hero">
  <div class="container">
    <h1>הקופות והקרנות שלך במקום אחד-  ומה <em>החשיפה</em> שלך למניות ולמט״ח</h1>
    <div class="info-cubes">
      <button type="button" class="info-cube" data-tip="exposure" aria-expanded="false" aria-controls="cubeTooltip">
        <div class="info-cube-icon">📊</div>
        <div class="info-cube-text">רואים בקליק את החשיפה למניות ולמט״ח</div>
      </button>
      <button type="button" class="info-cube" data-tip="privacy" aria-expanded="false" aria-controls="cubeTooltip">
        <div class="info-cube-icon">🔒</div>
        <div class="info-cube-text">בלי הרשמה – הכל נשמר רק אצלך</div>
      </button>
      <button type="button" class="info-cube" data-tip="workflow" aria-expanded="false" aria-controls="cubeTooltip">
        <div class="info-cube-icon">💡</div>
        <div class="info-cube-text">חפש ⬅ עדכן יתרה ⬅ עדכן בדשבורד</div>
      </button>
      <button type="button" class="info-cube" data-tip="source" aria-expanded="false" aria-controls="cubeTooltip">
        <div class="info-cube-icon">🗂️</div>
        <div class="info-cube-text">מקור הנתונים: גמל נט, פנסיה נט וביטוח נט</div>
      </button>
    </div>
    <div class="info-cube-tooltip" id="cubeTooltip" role="region" aria-live="polite" hidden></div>
  </div>
</section>

<section class="section" id="searchSection">
  <div class="container">
    <h2 class="section-title">מצא את הקופות שלך</h2>
    <div class="filters" id="filters">
      <button class="filter-btn active" data-filter="all" data-label="הכל">הכל</button>
      <button class="filter-btn" data-filter="gemelnet" data-label="גמל / השתלמות / גמל להשקעה">גמל / השתלמות / גמל להשקעה</button>
      <button class="filter-btn" data-filter="pensia" data-label="קרנות פנסיה">קרנות פנסיה</button>
      <button class="filter-btn" data-filter="bituach" data-label="פוליסות חיסכון / ביטוחי מנהלים">פוליסות חיסכון / ביטוחי מנהלים</button>
    </div>
    <div class="search-wrap">
      <input type="text" class="search-input" id="searchInput" placeholder="שם החברה או המסלול — לדוגמה: מנורה, הראל, מיטב, אלטשולר" autocomplete="off" />
      <div class="quick-editor" id="quickEditor"></div>
      <div class="search-results" id="searchResults"></div>
    </div>
  </div>
</section>

<section class="section" id="portfolioSection" style="display:none;">
  <div class="container">
    <h2 class="section-title"><span class="num">02</span>התיק שלך</h2>
    <div class="funds-list" id="fundsList"></div>
  </div>
</section>

<section class="dashboard" id="dashboard">
  <div class="container">
    <h2>דשבורד מצרפי</h2>
    <div class="subtitle">החישוב משוקלל לפי היתרות שהוזנו לכל מוצר</div>
    <div class="action-toolbar" aria-label="פעולות דוח">
      <button type="button" class="action-btn" onclick="saveAsPDF()">שמירה / הדפסה ל-PDF</button>
      <button type="button" class="action-btn" onclick="shareWhatsApp()">שליחה לווטסאפ</button>
      <button type="button" class="action-btn" onclick="openProjection()">סימולציה על בסיס תשואות עבר</button>
      <button type="button" class="action-btn" onclick="resetAllData()">איפוס הכל</button>
    </div>
    <div class="action-note">שמירה ל-PDF נפתחת דרך חלון ההדפסה של הדפדפן. בשל מגבלות דפדפן, שליחה לווטסאפ שולחת סיכום טקסטואלי וקישור, לא מצרפת PDF אוטומטית.</div>

    <div class="total-row">
      <div>
        <div class="total" id="totalBalance">-</div>
        <div class="total-label">יתרה כוללת שהוזנה</div>
      </div>
      <div class="source-mix" id="sourceMix"></div>
    </div>

    <div class="metric-grid">
      <div class="metric-card">
        <div class="metric-label">חשיפה למניות</div>
        <div class="metric-value"><span id="aggStock">0</span><span class="pct">%</span></div>
        <div class="metric-bar"><div class="metric-bar-fill stock-fill" id="aggStockBar"></div></div>
      </div>
      <div class="metric-card">
        <div class="metric-label">חשיפה לחו״ל</div>
        <div class="metric-value"><span id="aggForeign">0</span><span class="pct">%</span></div>
        <div class="metric-bar"><div class="metric-bar-fill foreign-fill" id="aggForeignBar"></div></div>
      </div>
      <div class="metric-card">
        <div class="metric-label">חשיפה למט״ח</div>
        <div class="metric-value"><span id="aggFx">0</span><span class="pct">%</span></div>
        <div class="metric-bar"><div class="metric-bar-fill fx-fill" id="aggFxBar"></div></div>
      </div>
      <div class="metric-card">
        <div class="metric-label">נכסים סחירים</div>
        <div class="metric-value"><span id="aggLiquid">0</span><span class="pct">%</span></div>
        <div class="metric-bar"><div class="metric-bar-fill liquid-fill" id="aggLiquidBar"></div></div>
      </div>
    </div>

    <div class="perf-wrap">
      <div class="perf-title">אינדיקציית ביצועי מסלולים</div>
      <div class="perf-note">מידע תומך בלבד: הנתונים משקפים את תשואות המסלולים הציבוריות לפי המאגר, ולא את התשואה האישית בפועל.</div>
      <div class="metric-grid">
        <div class="metric-card">
          <div class="metric-label">תשואה חודשית</div>
          <div class="metric-value"><span id="perfMonth">-</span><span class="pct">%</span></div>
        </div>
        <div class="metric-card">
          <div class="metric-label">מתחילת שנה</div>
          <div class="metric-value"><span id="perfYtd">-</span><span class="pct">%</span></div>
        </div>
        <div class="metric-card">
          <div class="metric-label">3 שנים מצטבר</div>
          <div class="metric-value"><span id="perf3y">-</span><span class="pct">%</span></div>
        </div>
        <div class="metric-card">
          <div class="metric-label">דמי ניהול אישיים</div>
          <div class="metric-value"><span id="feeSavings">לא הוזן</span><span class="pct" id="feeSavingsPct" style="display:none">%</span></div>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="details" id="detailsSection">
  <div class="container">
    <h2>פירוט לפי מוצר</h2>
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>מוצר</th>
            <th class="num">משקל</th>
            <th class="num">מניות</th>
            <th class="num">חו״ל</th>
            <th class="num">מט״ח</th>
            <th class="num">סחיר</th>
            <th class="num">חודשי</th>
            <th class="num">YTD</th>
            <th class="num">3Y מצטבר</th>
            <th class="num">דמי ניהול שהוזנו</th>
          </tr>
        </thead>
        <tbody id="detailsRows"></tbody>
      </table>
    </div>
  </div>
</section>

<!-- ====================================================================== -->
<!-- כרטיס "תרגיל היסטורי" — drop-in לדשבורד                                -->
<!-- הדבק את כל הבלוק הזה ב-index.html של הדשבורד, ממש לפני <footer>        -->
<!-- ====================================================================== -->
<section class="projection-section" id="projectionSection" hidden>
  <div class="container">
    <button type="button" class="proj-toggle" id="projToggle" aria-expanded="false" aria-controls="projBody">
      <span class="proj-toggle-icon" aria-hidden="true">📊</span>
      <span class="proj-toggle-text">
        <span class="proj-toggle-title">סימולציה על בסיס תשואות עבר</span>
        <span class="proj-toggle-sub">על סמך תשואות עבר, כך הסכומים עשויים להיראות לאורך זמן</span>
      </span>
      <span class="proj-toggle-chevron" aria-hidden="true">▾</span>
    </button>

    <div class="proj-body" id="projBody" hidden>
      <p class="proj-intro">
        הסימולציה מבוססת על תשואות עבר של 30 השנים האחרונות (1995–2024), עבור מסלולים בעלי פרופיל
        <strong id="projRiskLabel">—</strong>
        — בהתאם לחשיפה המנייתית בתיק שלך (<span id="projStockPct">—</span>%).
        <br><small>סיווג הסיכון: עד 10% מניות = נמוך, מעל 10% ועד 70% = בינוני, מעל 70% = גבוה.</small>
      </p>

      <div class="proj-empty" id="projEmpty">
        עדיין לא הוספת קופות לתיק. חזור למעלה, חפש קופה והזן יתרה — ואז נוכל להריץ סימולציה על בסיס תשואות עבר.
      </div>

      <div class="proj-form" id="projForm" hidden>
        <div class="proj-input-grid">
          <label class="proj-input">
            <span class="proj-input-label">סכום התחלתי (₪)</span>
            <input type="number" id="projInitial" inputmode="numeric" min="0" />
            <span class="proj-input-hint">נטען מהיתרה הכוללת בדשבורד</span>
          </label>
          <label class="proj-input">
            <span class="proj-input-label">הפקדה חודשית (₪)</span>
            <input type="number" id="projMonthly" inputmode="numeric" min="0" value="0" />
            <span class="proj-input-hint">לא חובה — השאר 0 לבדיקה של היתרה הקיימת בלבד</span>
          </label>
          <label class="proj-input">
            <span class="proj-input-label">דמי ניהול שנתיים (%)</span>
            <input type="number" id="projFee" step="0.01" min="0" value="0.7" />
            <span class="proj-input-hint">נטען אוטומטית אם הזנת בכרטיסי הקופות</span>
          </label>
          <label class="proj-input">
            <span class="proj-input-label">אופק זמן (שנים)</span>
            <input type="number" id="projYears" inputmode="numeric" min="1" max="30" step="1" value="20" />
            <span class="proj-input-hint">בחר 1–30 שנים, בהתאם לטווח הנתונים ההיסטוריים (1995–2024)</span>
          </label>
        </div>

        <div class="proj-action-row">
          <button type="button" class="proj-run-btn" id="projRunBtn">הרץ סימולציה</button>
        </div>
      </div>

      <div class="proj-results" id="projResults" hidden>
        <div class="proj-summary">
          <div class="proj-metric proj-metric-main">
            <div class="proj-metric-label">סכום מחושב בסוף התקופה</div>
            <div class="proj-metric-value" id="projFinal">—</div>
          </div>
          <div class="proj-metric">
            <div class="proj-metric-label">סך הפקדות בתקופה</div>
            <div class="proj-metric-value" id="projContrib">—</div>
          </div>
          <div class="proj-metric">
            <div class="proj-metric-label">צבירה מתשואה (נטו)</div>
            <div class="proj-metric-value" id="projInterest">—</div>
          </div>
          <div class="proj-metric proj-metric-warn">
            <div class="proj-metric-label">השנה הגרועה בתקופה</div>
            <div class="proj-metric-value" id="projWorstYear">—</div>
          </div>
        </div>

        <details class="proj-yearly">
          <summary>פירוט שנה-שנה</summary>
          <div class="proj-table-wrap">
            <table>
              <thead>
                <tr>
                  <th>שנה (היסטורית)</th>
                  <th class="num">תשואה שנתית</th>
                  <th class="num">יתרה בסוף השנה</th>
                </tr>
              </thead>
              <tbody id="projYearlyBody"></tbody>
            </table>
          </div>
        </details>

        <p class="proj-disclaimer">
          <strong>גילוי נאות:</strong> הסימולציה מבוססת על נתוני עבר בלבד ומיועדת להמחשה וחינוך פיננסי. אין לראות בה תחזית, הבטחה לתשואה, ייעוץ השקעות, ייעוץ פנסיוני או המלצה. ביצועי עבר אינם מנבאים את העתיד.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  /* === Historical Projection card === */
  .projection-section {
    padding: 8px 0 56px;
    background: var(--bg);
  }
  .projection-section .container { padding: 0 28px; }

  .proj-toggle {
    width: 100%;
    display: flex;
    align-items: center;
    gap: 16px;
    background: var(--card);
    border: 1px solid var(--line);
    border-radius: var(--r-md);
    padding: 18px 22px;
    text-align: right;
    cursor: pointer;
    box-shadow: var(--shadow-sm);
    transition: box-shadow .2s, border-color .2s;
    font: inherit;
    color: inherit;
  }
  .proj-toggle:hover { box-shadow: var(--shadow-md); border-color: var(--accent); }
  .proj-toggle[aria-expanded="true"] {
    border-color: var(--accent);
    border-bottom-left-radius: 0;
    border-bottom-right-radius: 0;
  }
  .proj-toggle-icon { font-size: 22px; line-height: 1; flex-shrink: 0; }
  .proj-toggle-text { display: flex; flex-direction: column; gap: 3px; flex: 1; min-width: 0; }
  .proj-toggle-title { font-size: 17px; font-weight: 700; color: var(--ink); letter-spacing: -0.01em; }
  .proj-toggle-sub { font-size: 13px; color: var(--ink-muted); font-weight: 400; line-height: 1.4; }
  .proj-toggle-chevron {
    font-size: 18px;
    color: var(--ink-muted);
    transition: transform .25s;
    flex-shrink: 0;
  }
  .proj-toggle[aria-expanded="true"] .proj-toggle-chevron { transform: rotate(180deg); }

  .proj-body {
    background: var(--card);
    border: 1px solid var(--accent);
    border-top: none;
    border-bottom-left-radius: var(--r-md);
    border-bottom-right-radius: var(--r-md);
    padding: 22px;
    box-shadow: var(--shadow-md);
  }
  .proj-intro {
    margin: 0 0 18px;
    font-size: 14px;
    color: var(--ink-soft);
    line-height: 1.6;
    background: var(--accent-soft);
    padding: 12px 14px;
    border-radius: var(--r-sm);
    border-right: 3px solid var(--accent);
  }
  .proj-intro strong { color: var(--accent-dark); font-weight: 700; }

  .proj-empty {
    background: var(--bg-soft);
    border: 1px dashed var(--line);
    border-radius: var(--r-sm);
    padding: 22px;
    color: var(--ink-muted);
    text-align: center;
    font-size: 14px;
    line-height: 1.6;
  }

  .proj-input-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 14px;
    margin-bottom: 18px;
  }
  .proj-input { display: flex; flex-direction: column; gap: 5px; }
  .proj-input-label { font-size: 13px; font-weight: 600; color: var(--ink-soft); }
  .proj-input input[type="number"] {
    font: inherit;
    font-size: 15px;
    border: 1px solid var(--line);
    border-radius: var(--r-sm);
    padding: 10px 12px;
    background: var(--card-tint);
    color: var(--ink);
    transition: border-color .15s, background .15s;
    width: 100%;
    box-sizing: border-box;
    font-variant-numeric: tabular-nums;
  }
  .proj-input input[type="number"]:focus {
    outline: none;
    border-color: var(--accent);
    background: var(--card);
    box-shadow: 0 0 0 3px var(--accent-soft);
  }
  .proj-input-hint { font-size: 11.5px; color: var(--ink-faint); line-height: 1.4; }

  .proj-years { display: flex; gap: 6px; flex-wrap: wrap; }
  .proj-year-btn {
    flex: 1 1 auto;
    min-width: 70px;
    font: inherit;
    font-size: 13px;
    font-weight: 600;
    padding: 10px 10px;
    border: 1px solid var(--line);
    background: var(--card-tint);
    color: var(--ink-soft);
    border-radius: var(--r-sm);
    cursor: pointer;
    transition: all .15s;
  }
  .proj-year-btn:hover { border-color: var(--accent); color: var(--accent-dark); }
  .proj-year-btn.active {
    background: var(--accent);
    color: #fff;
    border-color: var(--accent);
    box-shadow: var(--shadow-xs);
  }

  .proj-action-row { display: flex; justify-content: center; }
  .proj-run-btn {
    font: inherit;
    font-size: 15px;
    font-weight: 700;
    background: var(--accent);
    color: #fff;
    border: none;
    border-radius: var(--r-sm);
    padding: 12px 32px;
    cursor: pointer;
    transition: background .15s, transform .1s, box-shadow .15s;
    box-shadow: var(--shadow-sm);
    letter-spacing: -0.01em;
  }
  .proj-run-btn:hover { background: var(--accent-dark); box-shadow: var(--shadow-md); }
  .proj-run-btn:active { transform: translateY(1px); }

  .proj-results { margin-top: 24px; padding-top: 22px; border-top: 1px solid var(--line-soft); }
  .proj-summary {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 12px;
    margin-bottom: 18px;
  }
  .proj-metric {
    background: var(--card-tint);
    border: 1px solid var(--line);
    border-radius: var(--r-sm);
    padding: 14px 16px;
  }
  .proj-metric-main {
    background: var(--accent-soft);
    border-color: var(--accent-soft-2);
  }
  .proj-metric-main .proj-metric-value { color: var(--accent-dark); }
  .proj-metric-warn { background: #fff7ed; border-color: #fed7aa; }
  .proj-metric-warn .proj-metric-value { color: var(--warning); }
  .proj-metric-label {
    font-size: 12px;
    color: var(--ink-muted);
    font-weight: 600;
    margin-bottom: 6px;
    letter-spacing: 0.01em;
  }
  .proj-metric-value {
    font-size: 22px;
    font-weight: 800;
    color: var(--ink);
    letter-spacing: -0.015em;
    line-height: 1.2;
    font-variant-numeric: tabular-nums;
  }

  .proj-yearly { margin-top: 10px; }
  .proj-yearly summary {
    cursor: pointer;
    font-size: 13px;
    font-weight: 600;
    color: var(--accent-dark);
    padding: 10px 0;
    list-style: none;
    user-select: none;
  }
  .proj-yearly summary::-webkit-details-marker { display: none; }
  .proj-yearly summary::before {
    content: '▸';
    display: inline-block;
    margin-left: 6px;
    transition: transform .2s;
  }
  .proj-yearly[open] summary::before { transform: rotate(90deg); }
  .proj-table-wrap {
    margin-top: 8px;
    border: 1px solid var(--line);
    border-radius: var(--r-sm);
    overflow: hidden;
  }
  .proj-table-wrap table { width: 100%; border-collapse: collapse; font-size: 13px; }
  .proj-table-wrap th, .proj-table-wrap td {
    padding: 9px 14px;
    border-bottom: 1px solid var(--line-soft);
    text-align: right;
  }
  .proj-table-wrap tbody tr:last-child td { border-bottom: none; }
  .proj-table-wrap th {
    background: var(--bg-soft);
    font-weight: 700;
    color: var(--ink-soft);
    font-size: 12px;
  }
  .proj-table-wrap td.num { font-variant-numeric: tabular-nums; }
  .proj-table-wrap tr.bad-year { background: var(--danger-soft); }
  .proj-table-wrap tr.bad-year td { color: var(--danger); font-weight: 700; }
  .proj-pos { color: var(--pos); font-weight: 600; }
  .proj-neg { color: var(--neg); font-weight: 600; }

  .proj-disclaimer {
    margin-top: 18px;
    padding: 12px 14px;
    background: var(--bg-soft);
    border-radius: var(--r-sm);
    font-size: 12px;
    color: var(--ink-muted);
    line-height: 1.65;
  }
  .proj-disclaimer strong { color: var(--ink-soft); }

  @media (max-width: 640px) {
    .projection-section .container { padding: 0 20px; }
    .proj-toggle { padding: 14px 16px; gap: 12px; }
    .proj-toggle-title { font-size: 15px; }
    .proj-toggle-sub { font-size: 12px; }
    .proj-body { padding: 16px; }
    .proj-metric-value { font-size: 18px; }
    .proj-run-btn { width: 100%; }
  }
</style>

<script>
(function() {
  'use strict';

  // === נתוני תשואות היסטוריים (1995-2024) — זהים למחשבון הקיים ===
  const RAW_YIELDS = {
    low:    [22.66, 6.42, 13.62, 15.60, 0.18, 9.39, 1.67, 5.01, 6.11, 5.12, 3.33, 5.27, 8.12, -0.30, -0.37, 7.07, 10.05, 5.98, 0.61, 9.16, 1.05, 2.83, 6.99, -0.06, 13.25, 10.47, 3.05, -13.89, 8.60, 5.03],
    medium: [30.82, 14.37, 23.60, 22.47, 9.89, 0.71, -4.99, -7.81, 17.20, 8.09, 4.06, 10.29, 6.78, -14.72, 11.58, 11.81, 6.85, 10.61, 15.11, 11.89, 1.21, 7.30, 14.10, -2.55, 22.29, 14.80, 15.54, -16.96, 17.26, 14.61],
    high:   [35.83, 20.56, 30.78, 27.00, 17.98, -6.46, -10.11, -18.26, 25.56, 10.11, 4.63, 14.24, 5.95, -30.89, 22.23, 14.18, 3.49, 14.60, 28.03, 13.24, 1.37, 10.66, 19.73, -3.81, 29.05, 17.35, 25.18, -18.02, 23.84, 22.23]
  };
  const START_YEAR = 1995;
  const RISK_LABELS = { low: 'סיכון נמוך', medium: 'סיכון בינוני', high: 'סיכון גבוה' };

  // === מיפוי DOM ===
  const $ = (id) => document.getElementById(id);
  const section       = $('projectionSection');
  const toggle        = $('projToggle');
  const body          = $('projBody');
  const riskLabel     = $('projRiskLabel');
  const stockPctEl    = $('projStockPct');
  const empty         = $('projEmpty');
  const form          = $('projForm');
  const initialInput  = $('projInitial');
  const monthlyInput  = $('projMonthly');
  const feeInput      = $('projFee');
  const yearsInput   = $('projYears');
  const runBtn        = $('projRunBtn');
  const results       = $('projResults');
  const finalEl       = $('projFinal');
  const contribEl     = $('projContrib');
  const interestEl    = $('projInterest');
  const worstEl       = $('projWorstYear');
  const yearlyBody    = $('projYearlyBody');

  // === Helpers ===
  const fmtILS = (n) => Math.round(n).toLocaleString('he-IL', {
    style: 'currency', currency: 'ILS', maximumFractionDigits: 0
  });
  const parseNum = (txt) => {
    if (!txt) return NaN;
    const cleaned = String(txt).replace(/[^\d.\-]/g, '');
    const n = parseFloat(cleaned);
    return Number.isFinite(n) ? n : NaN;
  };

  // מיפוי חשיפה מנייתית → מסלול היסטורי
  function pickRiskTrack(stockPct) {
    if (stockPct <= 10) return 'low';
    if (stockPct <= 70) return 'medium';
    return 'high';
  }

  // קריאת מצב הדשבורד מתוך ה-DOM שכבר רונדר
  function readDashboardState() {
    const totalText = $('totalBalance') ? $('totalBalance').textContent : '';
    const stockText = $('aggStock')     ? $('aggStock').textContent     : '';
    const feeText   = $('feeSavings')   ? $('feeSavings').textContent   : '';
    const total    = parseNum(totalText);
    const stockPct = parseNum(stockText);
    const fee      = parseNum(feeText);
    return {
      total:        Number.isFinite(total) && total > 0 ? total : 0,
      stockPct:     Number.isFinite(stockPct) ? stockPct : 0,
      fee:          Number.isFinite(fee) && fee > 0 ? fee : null,
      hasPortfolio: Number.isFinite(total) && total > 0
    };
  }

  // טעינה מחדש של ברירות מחדל מתוך הדשבורד (בלי לדרוס מה שהמשתמש ערך)
  function refreshPrefill() {
    const state = readDashboardState();
    if (state.hasPortfolio) {
      empty.hidden = true;
      form.hidden = false;
      const track = pickRiskTrack(state.stockPct);
      riskLabel.textContent = RISK_LABELS[track];
      stockPctEl.textContent = state.stockPct.toFixed(1);
      if (!initialInput.dataset.userEdited) {
        initialInput.value = Math.round(state.total);
      }
      if (state.fee !== null && !feeInput.dataset.userEdited) {
        feeInput.value = state.fee.toFixed(2);
      }
    } else {
      empty.hidden = false;
      form.hidden = true;
      results.hidden = true;
      riskLabel.textContent = '—';
      stockPctEl.textContent = '—';
    }
  }

  // סימון שדות שהמשתמש ערך ידנית — כדי לא לדרוס בעדכון חוזר
  [initialInput, feeInput].forEach(el => {
    el.addEventListener('input', () => { el.dataset.userEdited = '1'; });
  });

  // === בחירת אופק זמן (1–30 שנים) ===
  function getSelectedYears() {
    const n = parseInt(yearsInput.value, 10);
    if (!Number.isFinite(n) || n < 1) return 1;
    if (n > 30) return 30;
    return n;
  }
  yearsInput.addEventListener('blur', () => {
    const clamped = getSelectedYears();
    if (String(clamped) !== yearsInput.value) yearsInput.value = String(clamped);
  });

  // === קיפול/פתיחה של הכרטיס ===
  toggle.addEventListener('click', () => {
    const expanded = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!expanded));
    body.hidden = expanded;
    if (!expanded) refreshPrefill();
  });

  // === מנוע הסימולציה (זהה לוגית למחשבון של דורון) ===
  function simulate({ initial, monthly, years, feePct, yields }) {
    let amount = initial;
    let contrib = initial;
    const yearly = [];
    let worstYield = Infinity;
    let worstHistYear = null;
    const feeMonthly = (feePct / 100) / 12;

    for (let y = 0; y < years; y++) {
      const idx = y % yields.length;
      const annualYield = yields[idx] / 100;
      const monthlyYield = annualYield / 12;

      for (let m = 0; m < 12; m++) {
        amount += monthly;
        contrib += monthly;
        amount += amount * monthlyYield;
        amount -= amount * feeMonthly;
      }

      const histYear = START_YEAR + idx;
      yearly.push({ n: y + 1, histYear, yieldPct: yields[idx], balance: amount });

      if (yields[idx] < worstYield) {
        worstYield = yields[idx];
        worstHistYear = histYear;
      }
    }
    return {
      finalAmount: amount,
      totalContrib: contrib,
      totalInterest: amount - contrib,
      yearly, worstYield, worstHistYear
    };
  }

  // === רינדור התוצאות ===
  function renderResults(sim) {
    finalEl.textContent    = fmtILS(sim.finalAmount);
    contribEl.textContent  = fmtILS(sim.totalContrib);
    interestEl.textContent = fmtILS(sim.totalInterest);
    interestEl.className   = 'proj-metric-value ' + (sim.totalInterest >= 0 ? 'proj-pos' : 'proj-neg');
    worstEl.textContent    = `${sim.worstYield.toFixed(2)}% (${sim.worstHistYear})`;

    yearlyBody.innerHTML = '';
    sim.yearly.forEach(row => {
      const tr = document.createElement('tr');
      if (row.yieldPct < -5) tr.className = 'bad-year';
      const yieldClass = row.yieldPct >= 0 ? 'proj-pos' : 'proj-neg';
      tr.innerHTML = `
        <td>שנה ${row.n} <span style="color:var(--ink-faint);font-size:11px">(${row.histYear})</span></td>
        <td class="num ${yieldClass}">${row.yieldPct.toFixed(2)}%</td>
        <td class="num">${fmtILS(row.balance)}</td>
      `;
      yearlyBody.appendChild(tr);
    });
    results.hidden = false;
  }

  // === כפתור ההרצה ===
  runBtn.addEventListener('click', () => {
    const state = readDashboardState();
    if (!state.hasPortfolio) { refreshPrefill(); return; }

    const initial = parseFloat(initialInput.value) || 0;
    const monthly = parseFloat(monthlyInput.value) || 0;
    const feePct  = parseFloat(feeInput.value) || 0;
    const years   = getSelectedYears();
    const track   = pickRiskTrack(state.stockPct);

    const sim = simulate({ initial, monthly, years, feePct, yields: RAW_YIELDS[track] });
    renderResults(sim);
    results.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  });

  // פתיחה מהירה של אזור הסימולציה מתוך כפתור הדשבורד
  window.openProjection = function() {
    const state = readDashboardState();
    if (!state.hasPortfolio) return;
    section.hidden = false;
    toggle.setAttribute('aria-expanded', 'true');
    body.hidden = false;
    refreshPrefill();
    section.scrollIntoView({ behavior: 'smooth', block: 'start' });
  };

  // === הצגה / הסתרה לפי מצב התיק ===
  function syncSectionVisibility() {
    const state = readDashboardState();
    section.hidden = !state.hasPortfolio;
    if (state.hasPortfolio && toggle.getAttribute('aria-expanded') === 'true') {
      refreshPrefill();
    }
  }
  syncSectionVisibility();

  // === מעקב אחרי שינויים בדשבורד ===
  const observer = new MutationObserver(syncSectionVisibility);
  ['totalBalance', 'aggStock', 'feeSavings'].forEach(id => {
    const el = document.getElementById(id);
    if (el) observer.observe(el, { childList: true, characterData: true, subtree: true });
  });
})();
</script>
<!-- ====================================================================== -->
<!-- סוף הכרטיס                                                              -->
<!-- ====================================================================== -->


<footer>
  <div class="container">
    <strong>הבהרה מקצועית:</strong> הדשבורד מציג חשיפות וביצועים מתוך נתונים ציבוריים של המסלולים בלבד. הוא אינו מושך יתרות אישיות ממסלקה פנסיונית, אינו מחליף ייעוץ פנסיוני או פיננסי, ואינו מציג המלצה. "חשיפה לחו״ל" אינה שקולה בהכרח ל"מניות חו״ל", משום שבקבצים היא כוללת גם חשיפות זרות שאינן מנייתיות. נתוני התשואה הם נתוני מסלול ציבוריים: חודשית, מתחילת שנה ו-3 שנים מצטבר. דמי הניהול בדשבורד מבוססים רק על הנתון שהוזן ידנית עבור כל מוצר, ולא על דמי הניהול הממוצעים במאגר.
    <div class="data-update-meta" id="dataUpdateMeta"></div>
    <div class="developer-credit">פותח ע"י דורון שרייבמן</div>
  </div>
</footer>

<div class="loading" id="loadingState">טוען נתונים...</div>

<script>
let DATA = [];
let META = {};
let DATASET_MAX_PERIOD = null;
let activeFilter = 'all';
let userFunds = [];
let CURRENT_EDIT_FUND = null;
let LAST_FOCUSED_UID = null;
let QUICK_RETURN_VISIBLE = false;
const balanceInputTimers = {};
const feeInputTimers = {};
const STORAGE_KEY = 'pension_dashboard_funds_v3';

const SOURCE_LABEL = {
  gemelnet: 'גמל',
  pensia: 'פנסיה',
  bituach: 'ביטוח',
};
const SOURCE_DOT_COLOR = {
  gemelnet: '#60a5fa',
  pensia: '#c084fc',
  bituach: '#4ade80',
};

function escapeHtml(value) {
  return String(value ?? '')
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#039;');
}
function fmtPeriod(yyyymm) {
  if (!yyyymm) return '-';
  const s = String(yyyymm);
  return s.slice(4, 6) + '/' + s.slice(0, 4);
}
function monthDiff(periodA, periodB) {
  if (!periodA || !periodB) return 0;
  const a = String(periodA), b = String(periodB);
  const yA = parseInt(a.slice(0, 4), 10), mA = parseInt(a.slice(4, 6), 10);
  const yB = parseInt(b.slice(0, 4), 10), mB = parseInt(b.slice(4, 6), 10);
  return (yB - yA) * 12 + (mB - mA);
}
function fmtPct(value, digits = 1) {
  return Number.isFinite(value) ? value.toFixed(digits) + '%' : '-';
}
function fmtNum(value, digits = 1) {
  return Number.isFinite(value) ? value.toFixed(digits) : '-';
}
function fmtCurrency(value) {
  return '₪' + Math.round(value).toLocaleString('he-IL');
}
function normalizeText(value) {
  return String(value || '')
    .toLowerCase()
    .replace(/[״"'`׳]/g, '')
    .replace(/[&]/g, ' and ')
    .replace(/[.,()\/\_]/g, ' ')
    .replace(/[-\u2013\u2014]/g, ' ')
    .replace(/\s+/g, ' ')
    .trim();
}
function compactText(value) {
  return normalizeText(value).replace(/[^a-z0-9א-ת]/g, '');
}
let LAST_SEARCH_TOTAL = 0;
function withRuntimeFlags(rec) {
  const monthsBehind = monthDiff(rec.report_period, DATASET_MAX_PERIOD);
  return {
    ...rec,
    months_behind: monthsBehind,
    is_stale: monthsBehind > 3,
    is_micro_fund: (Number(rec.total_assets) || 0) < 10,
  };
}
function metricClass(value) {
  if (!Number.isFinite(value)) return '';
  if (value > 0) return 'positive';
  if (value < 0) return 'negative';
  return '';
}

function searchFunds(query, limit = Infinity, filterOverride = activeFilter, allowEmptyAll = false) {
  const q = normalizeText(query);
  const qCompact = compactText(query);
  LAST_SEARCH_TOTAL = 0;
  const filter = filterOverride || 'all';
  if (!q && filter === 'all' && !allowEmptyAll) return [];
  const scored = [];
  for (const rec of DATA) {
    if (filter !== 'all' && rec.source !== filter) continue;

    // בגרסת הדשבורד מציגים רק מוצרים פעילים עם נכסים, כדי שלא ייכנסו חישובי חשיפה ריקים.
    if (!rec.is_active || (Number(rec.total_assets) || 0) <= 0) continue;

    const name = normalizeText(rec.fund_name);
    const mgr = normalizeText(rec.managing_corp + ' ' + rec.parent_company_name + ' ' + rec.controlling_corp);
    const cls = normalizeText(rec.classification + ' ' + rec.specialization + ' ' + rec.sub_specialization);
    const idText = normalizeText(rec.fund_id);
    const fullText = `${name} ${mgr} ${cls} ${idText}`;
    const fullCompact = compactText(fullText);

    let score = 0;
    if (!q) {
      score = 1;
    } else {
      if (name === q) score += 30;
      else if (name.startsWith(q)) score += 18;
      else if (name.includes(q)) score += 12;

      if (mgr === q) score += 35;
      else if (mgr.startsWith(q)) score += 22;
      else if (mgr.includes(q)) score += 16;
      if (cls.includes(q)) score += 4;
      if (idText === q) score += 25;

      // מאפשר חיפוש גם כאשר המשתמש מקליד S&P500 / SP500 / אס אנד פי בצורה לא זהה לשם במאגר.
      if (qCompact && fullCompact.includes(qCompact)) score += 10;

      const parts = q.split(' ').filter(Boolean);
      if (parts.length > 1 && parts.every(p => fullText.includes(p) || fullCompact.includes(compactText(p)))) score += 8;

      if (score === 0) continue;
    }
    scored.push({ rec, score, total: Number(rec.total_assets) || 0 });
  }

  const sourceRank = { gemelnet: 1, pensia: 2, bituach: 3 };
  scored.sort((a, b) => {
    if (b.score !== a.score) return b.score - a.score;
    const sr = (sourceRank[a.rec.source] || 9) - (sourceRank[b.rec.source] || 9);
    if (sr !== 0) return sr;
    const ma = normalizeText(a.rec.managing_corp || a.rec.parent_company_name || a.rec.controlling_corp || '');
    const mb = normalizeText(b.rec.managing_corp || b.rec.parent_company_name || b.rec.controlling_corp || '');
    if (ma !== mb) return ma.localeCompare(mb, 'he');
    const ca = normalizeText(a.rec.classification || a.rec.specialization || '');
    const cb = normalizeText(b.rec.classification || b.rec.specialization || '');
    if (ca !== cb) return ca.localeCompare(cb, 'he');
    return normalizeText(a.rec.fund_name).localeCompare(normalizeText(b.rec.fund_name), 'he');
  });
  LAST_SEARCH_TOTAL = scored.length;
  return scored.slice(0, Number.isFinite(limit) ? limit : scored.length).map(x => withRuntimeFlags(x.rec));
}

function normalizeUserFee(value) {
  if (value === '' || value === null || value === undefined) return null;
  const num = Number(value);
  return Number.isFinite(num) && num >= 0 ? num : null;
}
function fmtUserFee(value) {
  return Number.isFinite(value) ? value.toFixed(2) + '%' : 'לא הוזן';
}
function saveState() {
  const minimal = userFunds.map(item => ({
    uid: item.fund.uid,
    balance: item.balance || 0,
    management_fee: Number.isFinite(item.managementFee) ? item.managementFee : null,
    updated_at: item.updated_at || new Date().toISOString()
  }));
  localStorage.setItem(STORAGE_KEY, JSON.stringify(minimal));
}
function loadState() {
  try {
    const saved = JSON.parse(localStorage.getItem(STORAGE_KEY) || '[]');
    userFunds = [];
    for (const item of saved) {
      const rec = DATA.find(r => r.uid === item.uid);
      if (rec) userFunds.push({
        fund: withRuntimeFlags(rec),
        balance: Number(item.balance) || 0,
        managementFee: normalizeUserFee(item.management_fee),
        updated_at: item.updated_at
      });
    }
  } catch (e) {
    userFunds = [];
  }
}

function renderDatasetMeta() {
  const el = document.getElementById('dataUpdateMeta');
  if (el) {
    el.textContent = `תאריך עדכון נתוני המאגר: ${fmtPeriod(DATASET_MAX_PERIOD)}`;
  }
}

function renderFunds() {
  const section = document.getElementById('portfolioSection');
  const wrap = document.getElementById('fundsList');
  if (section) section.style.display = 'none';
  if (wrap) wrap.innerHTML = '';
  QUICK_RETURN_VISIBLE = false;
  updateFloatingSearchButton();
  renderDashboard();
}

function weightedMetric(key, totalBalance) {
  let covered = 0;
  let sum = 0;
  for (const item of userFunds) {
    const balance = Number(item.balance) || 0;
    const value = Number(item.fund[key]);
    if (balance > 0 && Number.isFinite(value)) {
      covered += balance;
      sum += balance * value;
    }
  }
  return covered > 0 ? sum / covered : null;
}
function weightedUserManagementFee(totalBalance) {
  let sum = 0;
  let covered = 0;
  for (const item of userFunds) {
    const balance = Number(item.balance) || 0;
    if (balance <= 0) continue;
    if (!Number.isFinite(item.managementFee)) return null;
    covered += balance;
    sum += balance * item.managementFee;
  }
  return covered > 0 ? sum / covered : null;
}
function setFeeMetric(value) {
  const el = document.getElementById('feeSavings');
  const pct = document.getElementById('feeSavingsPct');
  if (Number.isFinite(value)) {
    el.textContent = value.toFixed(2);
    if (pct) pct.style.display = '';
  } else {
    el.textContent = 'לא הוזן';
    if (pct) pct.style.display = 'none';
  }
  el.className = '';
}
function setMetric(id, value, digits = 1) {
  const el = document.getElementById(id);
  el.textContent = Number.isFinite(value) ? value.toFixed(digits) : '-';
  el.className = metricClass(value);
}

function renderDashboard() {
  const activeItems = userFunds.filter(item => (Number(item.balance) || 0) > 0);
  const totalBalance = activeItems.reduce((sum, item) => sum + (Number(item.balance) || 0), 0);
  if (totalBalance <= 0) {
    document.getElementById('dashboard').style.display = 'none';
    document.getElementById('detailsSection').style.display = 'none';
    return;
  }

  const aggStock = weightedMetric('stock_pct', totalBalance);
  const aggForeign = weightedMetric('foreign_pct', totalBalance);
  const aggFx = weightedMetric('fx_pct', totalBalance);
  const aggLiquid = weightedMetric('liquid_pct', totalBalance);
  const perfMonth = weightedMetric('monthly_yield', totalBalance);
  const perfYtd = weightedMetric('ytd_yield', totalBalance);
  const perf3y = weightedMetric('yield_trailing_3yrs', totalBalance);
  const feeSavings = weightedUserManagementFee(totalBalance);

  document.getElementById('totalBalance').textContent = fmtCurrency(totalBalance);
  setMetric('aggStock', aggStock, 1);
  setMetric('aggForeign', aggForeign, 1);
  setMetric('aggFx', aggFx, 1);
  setMetric('aggLiquid', aggLiquid, 1);
  setMetric('perfMonth', perfMonth, 2);
  setMetric('perfYtd', perfYtd, 2);
  setMetric('perf3y', perf3y, 2);
  setFeeMetric(feeSavings);

  const barPairs = [
    ['aggStockBar', aggStock],
    ['aggForeignBar', aggForeign],
    ['aggFxBar', aggFx],
    ['aggLiquidBar', aggLiquid],
  ];
  for (const [id, value] of barPairs) {
    document.getElementById(id).style.width = Math.max(0, Math.min(100, Number(value) || 0)) + '%';
  }

  const sourceTotals = {};
  for (const item of activeItems) {
    const src = item.fund.source;
    sourceTotals[src] = (sourceTotals[src] || 0) + (Number(item.balance) || 0);
  }
  document.getElementById('sourceMix').innerHTML = Object.entries(sourceTotals)
    .sort((a, b) => b[1] - a[1])
    .map(([src, val]) => `<div class="source-mix-item"><span class="dot" style="background:${SOURCE_DOT_COLOR[src]}"></span>${SOURCE_LABEL[src]}: ${(val / totalBalance * 100).toFixed(0)}%</div>`)
    .join('');

  const rows = activeItems
    .slice()
    .sort((a, b) => (Number(b.balance) || 0) - (Number(a.balance) || 0))
    .map(item => {
      const f = item.fund;
      const balance = Number(item.balance) || 0;
      const weight = balance / totalBalance * 100;
      return `
        <tr>
          <td><span class="source-tag ${escapeHtml(f.source)}">${SOURCE_LABEL[f.source]}</span> ${escapeHtml(f.fund_name)}</td>
          <td class="num">${weight.toFixed(1)}%</td>
          <td class="num">${fmtPct(f.stock_pct, 1)}</td>
          <td class="num">${fmtPct(f.foreign_pct, 1)}</td>
          <td class="num">${fmtPct(f.fx_pct, 1)}</td>
          <td class="num">${fmtPct(f.liquid_pct, 1)}</td>
          <td class="num ${metricClass(f.monthly_yield)}">${fmtPct(f.monthly_yield, 2)}</td>
          <td class="num ${metricClass(f.ytd_yield)}">${fmtPct(f.ytd_yield, 2)}</td>
          <td class="num ${metricClass(f.yield_trailing_3yrs)}">${fmtPct(f.yield_trailing_3yrs, 2)}</td>
          <td class="num">${fmtUserFee(item.managementFee)}</td>
        </tr>
      `;
    }).join('');
  document.getElementById('detailsRows').innerHTML = rows;
  document.getElementById('dashboard').style.display = 'block';
  document.getElementById('detailsSection').style.display = 'block';
}



function getTotalBalance() {
  return userFunds.reduce((sum, item) => sum + (Number(item.balance) || 0), 0);
}
function hasReportData() {
  return userFunds.length > 0 && getTotalBalance() > 0;
}
function buildShareText() {
  const totalBalance = getTotalBalance();
  const aggStock = weightedMetric('stock_pct', totalBalance);
  const aggForeign = weightedMetric('foreign_pct', totalBalance);
  const aggFx = weightedMetric('fx_pct', totalBalance);
  const aggLiquid = weightedMetric('liquid_pct', totalBalance);
  const perfYtd = weightedMetric('ytd_yield', totalBalance);
  const feeSavings = weightedUserManagementFee(totalBalance);
  const lines = [
    'דשבורד חשיפות חיסכון',
    `יתרה כוללת שהוזנה: ${fmtCurrency(totalBalance)}`,
    `חשיפה למניות: ${fmtPct(aggStock, 1)}`,
    `חשיפה לחו״ל: ${fmtPct(aggForeign, 1)}`,
    `חשיפה למט״ח: ${fmtPct(aggFx, 1)}`,
    `נכסים סחירים: ${fmtPct(aggLiquid, 1)}`,
    `תשואה מתחילת שנה לפי מסלולים, משוקללת: ${fmtPct(perfYtd, 2)}`,
    `תשואה 3 שנים מצטברת לפי מסלולים, משוקללת: ${fmtPct(weightedMetric('yield_trailing_3yrs', totalBalance), 2)}`,
    `דמי ניהול אישיים משוקללים: ${Number.isFinite(feeSavings) ? fmtPct(feeSavings, 2) : 'לא הוזן'}`,
    '',
    'פירוט מוצרים:'
  ];
  const sorted = userFunds.slice().sort((a, b) => (Number(b.balance) || 0) - (Number(a.balance) || 0));
  for (const item of sorted) {
    const balance = Number(item.balance) || 0;
    if (balance <= 0) continue;
    const f = item.fund;
    const weight = totalBalance > 0 ? balance / totalBalance * 100 : 0;
    lines.push(`- ${SOURCE_LABEL[f.source]} | ${f.fund_name} | ${fmtCurrency(balance)} | משקל ${weight.toFixed(1)}% | מניות ${fmtPct(f.stock_pct, 1)} | חו״ל ${fmtPct(f.foreign_pct, 1)} | מט״ח ${fmtPct(f.fx_pct, 1)} | דמי ניהול ${fmtUserFee(item.managementFee)}`);
  }
  lines.push('');
  lines.push('הבהרה: הנתונים מבוססים על נתונים ציבוריים ויתרות שהוזנו ידנית. זה אינו ייעוץ או המלצה.');
  if (window.location && window.location.href) lines.push(`קישור לדשבורד: ${window.location.href}`);
  return lines.join('\n');
}
function saveAsPDF() {
  if (!hasReportData()) {
    alert('כדי לשמור PDF צריך להוסיף לפחות מוצר אחד ולהזין יתרה.');
    return;
  }
  renderDashboard();

  // Mobile Safari and some in-app browsers generate the PDF from the currently
  // rendered page before fully applying @media print. Force a visible light
  // report mode first, then open the print dialog.
  document.body.classList.add('pdf-mode');
  const dashboard = document.getElementById('dashboard');
  if (dashboard) dashboard.scrollIntoView({ behavior: 'auto', block: 'start' });

  window.setTimeout(() => {
    window.print();
    // Fallback: if afterprint does not fire on mobile, restore the screen later.
    window.setTimeout(() => document.body.classList.remove('pdf-mode'), 3500);
  }, 650);
}
window.addEventListener('afterprint', () => {
  window.setTimeout(() => document.body.classList.remove('pdf-mode'), 250);
});
async function shareWhatsApp() {
  if (!hasReportData()) {
    alert('כדי לשלוח לווטסאפ צריך להוסיף לפחות מוצר אחד ולהזין יתרה.');
    return;
  }
  const text = buildShareText();
  if (navigator.share) {
    try {
      await navigator.share({ title: 'דשבורד חשיפות חיסכון', text });
      return;
    } catch (error) {
      if (error && error.name === 'AbortError') return;
    }
  }
  const url = 'https://wa.me/?text=' + encodeURIComponent(text);
  window.open(url, '_blank', 'noopener,noreferrer');
}
function goToDashboard() {
  if (!hasReportData()) {
    alert('כדי לעבור לדשבורד צריך להוסיף לפחות מוצר אחד ולהזין יתרה.');
    return;
  }
  renderDashboard();
  const dashboard = document.getElementById('dashboard');
  if (dashboard) dashboard.scrollIntoView({ behavior: 'smooth', block: 'start' });
}
function resetAllData() {
  const hasData = userFunds.length > 0 || localStorage.getItem(STORAGE_KEY);
  if (!hasData) {
    prepareAnotherFund();
    return;
  }
  const ok = window.confirm('לאפס את כל הקופות, היתרות ודמי הניהול שהוזנו?');
  if (!ok) return;
  userFunds = [];
  CURRENT_EDIT_FUND = null;
  LAST_FOCUSED_UID = null;
  QUICK_RETURN_VISIBLE = false;
  localStorage.removeItem(STORAGE_KEY);
  const editor = document.getElementById('quickEditor');
  if (editor) {
    editor.classList.remove('active');
    editor.innerHTML = '';
  }
  const results = document.getElementById('searchResults');
  if (results) {
    results.classList.remove('active');
    results.innerHTML = '';
  }
  const input = document.getElementById('searchInput');
  if (input) input.value = '';
  renderDashboard();
  const section = document.getElementById('searchSection');
  if (section) section.scrollIntoView({ behavior: 'smooth', block: 'start' });
  window.setTimeout(() => { if (input) input.focus({ preventScroll: true }); }, 350);
}
window.saveAsPDF = saveAsPDF;
window.shareWhatsApp = shareWhatsApp;
window.goToDashboard = goToDashboard;
window.resetAllData = resetAllData;

function findUserFundIndex(uid) {
  return userFunds.findIndex(item => item.fund && item.fund.uid === uid);
}
function openQuickEditor(fund) {
  CURRENT_EDIT_FUND = withRuntimeFlags(fund);
  const existingIndex = findUserFundIndex(CURRENT_EDIT_FUND.uid);
  const existing = existingIndex >= 0 ? userFunds[existingIndex] : null;
  const balanceValue = existing && Number(existing.balance) > 0 ? Number(existing.balance) : '';
  const feeValue = existing && Number.isFinite(existing.managementFee) ? existing.managementFee : '';
  const sub = [CURRENT_EDIT_FUND.managing_corp, CURRENT_EDIT_FUND.classification, CURRENT_EDIT_FUND.specialization, CURRENT_EDIT_FUND.sub_specialization].filter(Boolean).join(' • ');
  const editor = document.getElementById('quickEditor');
  if (!editor) return;
  editor.innerHTML = `
    <div class="quick-editor-card">
      <div class="quick-editor-title"><span class="source-tag ${escapeHtml(CURRENT_EDIT_FUND.source)}">${SOURCE_LABEL[CURRENT_EDIT_FUND.source]}</span>${escapeHtml(CURRENT_EDIT_FUND.fund_name)}</div>
      <div class="quick-editor-meta">${escapeHtml(sub || '-')} • מספר קופה: ${escapeHtml(CURRENT_EDIT_FUND.fund_id || '')}</div>
      <div class="quick-editor-form">
        <label class="field-group">
          <span class="field-label">יתרה</span>
          <span class="inline-input">
            <input type="number" class="balance-input" id="quickBalanceInput" value="${escapeHtml(balanceValue)}" placeholder="0" min="0" inputmode="numeric" />
            <span class="currency">₪</span>
          </span>
        </label>
        <label class="field-group">
          <span class="field-label">דמי ניהול מצבירה</span>
          <span class="inline-input">
            <input type="number" class="fee-input" id="quickFeeInput" value="${escapeHtml(feeValue)}" placeholder="לא הוזן" min="0" step="0.01" inputmode="decimal" />
            <span class="currency">%</span>
          </span>
        </label>
        <button type="button" class="update-dashboard-btn" onclick="commitQuickUpdate()">עדכן בדשבורד</button>
        ${existing ? '<button type="button" class="quick-remove-btn" onclick="removeCurrentQuickFund()">הסר מהדשבורד</button>' : ''}
      </div>
      <div class="quick-editor-message" id="quickEditorMessage"></div>
    </div>
  `;
  editor.classList.add('active');
  editor.scrollIntoView({ behavior: 'smooth', block: 'center' });
  window.setTimeout(() => {
    const input = document.getElementById('quickBalanceInput');
    if (input) { input.focus({ preventScroll: true }); input.select(); }
  }, 120);
}
function commitQuickUpdate() {
  if (!CURRENT_EDIT_FUND) return;
  const balanceInput = document.getElementById('quickBalanceInput');
  const feeInput = document.getElementById('quickFeeInput');
  const balance = Math.max(0, Number(balanceInput ? balanceInput.value : 0) || 0);
  const fee = normalizeUserFee(feeInput ? feeInput.value : null);
  if (balance <= 0) {
    alert('יש להזין יתרה גבוהה מאפס כדי לעדכן את הדשבורד.');
    if (balanceInput) balanceInput.focus();
    return;
  }
  const idx = findUserFundIndex(CURRENT_EDIT_FUND.uid);
  const payload = {
    fund: CURRENT_EDIT_FUND,
    balance,
    managementFee: fee,
    updated_at: new Date().toISOString()
  };
  if (idx >= 0) userFunds[idx] = payload;
  else userFunds.push(payload);
  saveState();
  renderDashboard();
  const msg = document.getElementById('quickEditorMessage');
  if (msg) {
    msg.innerHTML = `
      <div>עודכן בדשבורד.</div>
      <div class="quick-action-row">
        <button type="button" class="add-another-btn" onclick="prepareAnotherFund()">הוסף קופה נוספת</button>
        <button type="button" class="go-dashboard-btn" onclick="goToDashboard()">עבור לדשבורד</button>
        <button type="button" class="reset-all-btn" onclick="resetAllData()">איפוס הכל</button>
      </div>
    `;
  }
  showResults(document.getElementById('searchInput').value || '');
  const wrap = document.getElementById('searchResults');
  if (wrap) wrap.classList.add('active');
  const input = document.getElementById('searchInput');
  if (input) input.focus({ preventScroll: true });
}
function removeCurrentQuickFund() {
  if (!CURRENT_EDIT_FUND) return;
  const idx = findUserFundIndex(CURRENT_EDIT_FUND.uid);
  if (idx >= 0) {
    userFunds.splice(idx, 1);
    saveState();
    renderDashboard();
    const msg = document.getElementById('quickEditorMessage');
    if (msg) msg.textContent = 'הוסר מהדשבורד.';
    showResults(document.getElementById('searchInput').value || '');
  }
}
function prepareAnotherFund() {
  CURRENT_EDIT_FUND = null;

  const editor = document.getElementById('quickEditor');
  if (editor) {
    editor.classList.remove('active');
    editor.innerHTML = '';
  }

  const results = document.getElementById('searchResults');
  if (results) {
    results.classList.remove('active');
    results.innerHTML = '';
  }

  const input = document.getElementById('searchInput');
  if (input) {
    input.value = '';
    input.scrollIntoView({ behavior: 'smooth', block: 'center' });
    window.setTimeout(() => {
      input.focus({ preventScroll: true });
    }, 350);
  } else {
    const searchSection = document.getElementById('searchSection');
    if (searchSection) searchSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }
}
window.prepareAnotherFund = prepareAnotherFund;
window.openQuickEditor = openQuickEditor;
window.commitQuickUpdate = commitQuickUpdate;
window.removeCurrentQuickFund = removeCurrentQuickFund;

function updateFloatingSearchButton() {
  // אין יותר כפתור צף קבוע. החזרה לחיפוש מופיעה רק בתוך שורת הקופה לאחר הזנת סכום.
}

function returnToSearch() {
  QUICK_RETURN_VISIBLE = false;
  updateFloatingSearchButton();
  const section = document.getElementById('searchSection');
  const input = document.getElementById('searchInput');
  const query = input ? input.value : '';
  if (query.trim()) showResults(query);
  if (section) section.scrollIntoView({ behavior: 'smooth', block: 'start' });
  window.setTimeout(() => {
    if (input) {
      input.focus({ preventScroll: true });
      if (!input.value.trim()) input.select();
    }
  }, 350);
}
window.returnToSearch = returnToSearch;

function focusFundInput(uid) {
  LAST_FOCUSED_UID = uid;
  renderFunds();
  window.setTimeout(() => {
    const card = document.querySelector(`.fund-card[data-uid="${CSS.escape(uid)}"]`);
    const input = document.querySelector(`.balance-input[data-uid="${CSS.escape(uid)}"]`);
    if (card) card.scrollIntoView({ behavior: 'smooth', block: 'center' });
    if (input) {
      input.focus({ preventScroll: true });
      input.select();
    }
  }, 50);
}

function addFund(fund) {
  openQuickEditor(fund);
}
function removeFund(idx) {
  if (balanceInputTimers[idx]) clearTimeout(balanceInputTimers[idx]);
  if (feeInputTimers[idx]) clearTimeout(feeInputTimers[idx]);
  if (userFunds[idx] && userFunds[idx].fund.uid === LAST_FOCUSED_UID) LAST_FOCUSED_UID = null;
  userFunds.splice(idx, 1);
  saveState();
  renderFunds();
}
function handleBalanceInput(idx, value, immediate = false) {
  if (balanceInputTimers[idx]) clearTimeout(balanceInputTimers[idx]);
  const run = () => updateBalance(idx, value);
  if (immediate) run();
  else balanceInputTimers[idx] = setTimeout(run, 650);
}
function updateBalance(idx, value) {
  if (!userFunds[idx]) return;
  const balance = Math.max(0, Number(value) || 0);
  userFunds[idx].balance = balance;
  userFunds[idx].updated_at = new Date().toISOString();
  saveState();
  const returnBtn = document.querySelector(`.return-search-btn[data-return-idx="${idx}"]`);
  if (returnBtn) returnBtn.style.display = balance > 0 ? 'inline-block' : 'none';
  QUICK_RETURN_VISIBLE = balance > 0;
  updateFloatingSearchButton();
  renderDashboard();
}
function handleFeeInput(idx, value, immediate = false) {
  if (feeInputTimers[idx]) clearTimeout(feeInputTimers[idx]);
  const run = () => updateManagementFee(idx, value);
  if (immediate) run();
  else feeInputTimers[idx] = setTimeout(run, 500);
}
function updateManagementFee(idx, value) {
  if (!userFunds[idx]) return;
  userFunds[idx].managementFee = normalizeUserFee(value);
  userFunds[idx].updated_at = new Date().toISOString();
  saveState();
  renderDashboard();
}
window.removeFund = removeFund;
window.updateBalance = updateBalance;
window.updateManagementFee = updateManagementFee;
window.handleBalanceInput = handleBalanceInput;
window.handleFeeInput = handleFeeInput;

function buildResultSummary(query, results) {
  const q = (query || '').trim();
  const counts = results.reduce((acc, r) => {
    acc.total += 1;
    acc[r.source] = (acc[r.source] || 0) + 1;
    return acc;
  }, { total: 0 });
  const countPills = ['gemelnet', 'pensia', 'bituach']
    .filter(src => counts[src])
    .map(src => `<span class="result-count-pill">${SOURCE_LABEL[src]}: ${counts[src]}</span>`)
    .join('');
  const title = q
    ? `נמצאו ${counts.total} מוצרים עבור החיפוש: "${escapeHtml(q)}"`
    : `מציג ${counts.total} מוצרים`;
  return `
    <div class="result-item result-summary">
      <div class="result-name">${title}</div>
      <div class="result-meta">
        <span>לחיצה על “עדכן” פותחת אזור מהיר להזנת יתרה ודמי ניהול</span>
        <span>כל התוצאות מוצגות בעמוד</span>
      </div>
      <div class="result-counts">${countPills}</div>
    </div>
  `;
}

function updateFilterCounts(query) {
  document.querySelectorAll('.filter-btn').forEach(btn => {
    const label = btn.dataset.label || btn.textContent.trim();
    btn.innerHTML = escapeHtml(label);
  });
}

function showResults(query) {
  updateFilterCounts(query);
  const results = searchFunds(query);
  const wrap = document.getElementById('searchResults');
  if (!results.length) {
    wrap.innerHTML = query.trim()
      ? '<div class="result-item result-summary"><div class="result-name">לא נמצאו תוצאות</div><div class="result-meta"><span>נסה לחפש רק את שם החברה, לדוגמה: מיטב / מנורה / הראל, או מספר קופה.</span></div></div>'
      : '';
    wrap.classList.toggle('active', Boolean(query.trim()));
    return;
  }
  const summary = buildResultSummary(query, results);
  wrap.innerHTML = summary + results.map(r => {
    const stale = r.is_stale ? `<span class="stale-badge">${r.months_behind} ח׳ ישן</span>` : '';
    const micro = r.is_micro_fund ? `<span class="micro-badge">קטנה</span>` : '';
    const exposure = `מניות ${fmtPct(r.stock_pct, 0)} • חו״ל ${fmtPct(r.foreign_pct, 0)} • מט״ח ${fmtPct(r.fx_pct, 0)}`;
    const manager = r.managing_corp || r.parent_company_name || '-';
    const alreadySelected = userFunds.some(item => item.fund.uid === r.uid);
    const selectedBadge = alreadySelected ? '<span class="selected-badge">כבר נוסף</span>' : '';
    return `
      <div class="result-item ${alreadySelected ? 'selected' : ''}" data-uid="${escapeHtml(r.uid)}">
        <div class="result-main-row">
          <div>
            <div class="result-name"><span class="source-tag ${escapeHtml(r.source)}">${SOURCE_LABEL[r.source]}</span>${escapeHtml(r.fund_name)} ${selectedBadge} ${stale} ${micro}</div>
            <div class="result-meta">
              <span>${escapeHtml(manager)}</span>
              <span>${escapeHtml(r.classification || '')}</span>
              <span>${exposure}</span>
              <span>נכסים: ₪${Math.round(Number(r.total_assets) || 0).toLocaleString('he-IL')} מיליון</span>
              <span>מספר: ${escapeHtml(r.fund_id || '')}</span>
            </div>
          </div>
          <button type="button" class="select-btn" data-uid="${escapeHtml(r.uid)}">עדכן</button>
        </div>
      </div>
    `;
  }).join('');
  wrap.classList.add('active');
  wrap.querySelectorAll('.select-btn[data-uid]').forEach(btn => {
    btn.addEventListener('click', (event) => {
      event.stopPropagation();
      const found = results.find(x => x.uid === btn.dataset.uid);
      if (found) addFund(found);
    });
  });
}

document.getElementById('searchInput').addEventListener('input', e => showResults(e.target.value));
document.getElementById('searchInput').addEventListener('focus', e => {
  if (activeFilter !== 'all' || e.target.value.trim()) showResults(e.target.value);
});
document.querySelectorAll('.filter-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    activeFilter = btn.dataset.filter;
    showResults(document.getElementById('searchInput').value);
  });
});
// התוצאות נשארות פתוחות כדי שהמשתמש יוכל לעבור על כל מוצרי החברה.


(async function init() {
  try {
    const res = await fetch('./funds_unified.json');
    if (!res.ok) throw new Error('HTTP ' + res.status);
    const payload = await res.json();
    DATA = Array.isArray(payload.funds) ? payload.funds : [];
    META = payload.meta || {};
    DATASET_MAX_PERIOD = META.dataset_max_period || Math.max(...DATA.map(x => Number(x.report_period) || 0));
    document.getElementById('loadingState').style.display = 'none';
    renderDatasetMeta();
    loadState();
    updateFilterCounts('');
    renderFunds();
  } catch (error) {
    document.getElementById('loadingState').textContent = 'שגיאה בטעינת הנתונים: ' + error.message;
    console.error(error);
  }
})();
</script>
<script>
/* v3.29: info cube tooltips - works on hover (desktop) and tap (mobile) */
(function() {
  const cubes = document.querySelectorAll('.info-cube');
  const tooltip = document.getElementById('cubeTooltip');
  if (!cubes.length || !tooltip) return;

  const tips = {
    exposure: '<strong>חשיפה למניות ולמט״ח</strong>חשיפה למניות = החלק מההשקעה שלך המושקע במניות (חשוף לתנודות שוק, פוטנציאל תשואה וסיכון גבוהים יותר). חשיפה למט״ח = החלק מההשקעה הצמוד למטבעות זרים (בעיקר דולר). שני המדדים האלה מאפשרים לך להבין במבט אחד את רמת הסיכון בחיסכון שלך.',
    privacy: '<strong>הנתונים שלך נשארים אצלך</strong>היתרות ודמי הניהול שהזנת נשמרים בדפדפן שלך. הם אינם נשמרים בשרת, ואינם משויכים לפרטים אישיים.',
    workflow: '<strong>איך עובדים עם הכלי</strong>1. חפש את שם החברה שמנהלת את הקופה שלך (אפשר למצוא בדוח השנתי שמגיע בדואר/מייל). 2. לחץ "עדכן" על הקופה הרצויה. 3. הזן יתרה ודמי ניהול בפועל. 4. לחץ "עדכן בדשבורד" כדי לראות את החשיפה הכוללת של כל החסכונות יחד.',
    source: '<strong>מקור הנתונים</strong>הנתונים מבוססים על קובצי המידע הציבוריים של רשות שוק ההון, הביטוח והחיסכון, מתוך מאגרי גמל נט, פנסיה נט וביטוח נט, כפי שפורסמו ב-data.gov.il. הדשבורד אינו מושך נתונים אישיים ממסלקה פנסיונית, אלא משתמש ביתרות ודמי ניהול שהמשתמש מזין ידנית.'
  };

  let activeCube = null;

  function closeTooltip() {
    cubes.forEach(c => c.setAttribute('aria-expanded', 'false'));
    tooltip.hidden = true;
    activeCube = null;
  }

  function openTooltip(cube) {
    const id = cube.dataset.tip;
    if (!tips[id]) return;
    cubes.forEach(c => c.setAttribute('aria-expanded', 'false'));
    cube.setAttribute('aria-expanded', 'true');
    tooltip.innerHTML = tips[id];
    tooltip.hidden = false;
    activeCube = cube;
  }

  cubes.forEach(cube => {
    cube.addEventListener('click', (e) => {
      e.stopPropagation();
      if (activeCube === cube) {
        closeTooltip();
      } else {
        openTooltip(cube);
      }
    });
  });

  // click outside closes tooltip
  document.addEventListener('click', (e) => {
    if (!e.target.closest('.info-cube') && !e.target.closest('.info-cube-tooltip')) {
      if (activeCube) closeTooltip();
    }
  });

  // ESC key closes tooltip
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && activeCube) closeTooltip();
  });
})();
</script>

<script>
(function() {
  const KEY = 'savings_dashboard_disclosure_accepted';
  const overlay = document.getElementById('entryDisclosureOverlay');
  const checkbox = document.getElementById('entryDisclosureCheck');
  const accept = document.getElementById('entryDisclosureAccept');
  if (!overlay || !checkbox || !accept) return;

  function storageGet() {
    try { return sessionStorage.getItem(KEY); } catch (e) { return null; }
  }
  function storageSet() {
    try { sessionStorage.setItem(KEY, '1'); } catch (e) {}
  }

  if (storageGet() === '1') {
    overlay.hidden = true;
    document.body.classList.remove('disclosure-gate-open');
    return;
  }

  document.body.classList.add('disclosure-gate-open');
  overlay.hidden = false;

  checkbox.addEventListener('change', function() {
    accept.disabled = !checkbox.checked;
  });

  accept.addEventListener('click', function() {
    if (!checkbox.checked) return;
    storageSet();
    overlay.hidden = true;
    document.body.classList.remove('disclosure-gate-open');
  });

  overlay.addEventListener('click', function(event) {
    if (event.target === overlay) {
      event.preventDefault();
    }
  });
})();
</script>

</body>
</html>
